# SpecPowers 桥接插件 v5 — 设计方案（决策锁定版）

> 状态：v4 的修订锁定版，依据用户 6 点反馈
> 核心变化：constitution 提为第一步（spec-kit）；编码规范重新定义为"项目结构一致性，禁无中生有"；实时卡锁定；plan/tasks 合并为单一 plan.md；补充与三框架原生流程的映射。

---

## 一、六点反馈逐条回应

### ① speckit.constitution 纳入，作为第一步
采纳。流程起点改为 **constitution（spec-kit）**。它定义项目"宪法"——尤其是**代码结构规范**（见⑥）：生成代码必须符合现有项目结构，不得无中生有。这是后续所有产物的强制基线。

新流程：**constitution → specify → plan → build → archive**

### ② 为什么 OpenSpec 出 spec 场景格式 + archive；spec-kit 出任务生成
坦白：这不是能力硬边界，而是**为"省 token"目标做的判断**。

- **OpenSpec 拥有 spec + archive**：立身之本是 **change-driven（变更驱动）**——spec 以"基线 + delta(change)"存在，`archive` 把 change 的 diff 合并回基线。这是省 token 骨架：每次迭代不必重写整篇 spec，只 archive 一个 diff。其 `#### WHEN/THEN` 场景语法天生为验收而写，简洁、可直接喂 verify。spec-kit 的 spec 格式更全但更冗长且静态（无原生 delta）。
- **spec-kit 拥有 tasks**：生成 `tasks.md`（可勾选、有序的任务拆解）本就是它的明确职责，成熟且对口。OpenSpec 没有"实现任务清单生成"能力（它的 tasks 是 change 层面，非实现任务）。

**诚实的替代方案**：spec-kit 也能从头产出 spec，你可让它跑到底（constitution→spec→tasks→implement），只借 OpenSpec 的 archive。我保留 OpenSpec 场景格式而非 spec-kit 的 spec 格式，**唯一理由是 delta/archive 的省 token 收益 + WHEN/THEN 可测性**。若省 token 非首要，纯 spec-kit 更省事。此分配服务于你的省 token 目标，非技术必然。

### ③ 先用实时卡方案
锁定：**coding-standards 在子代理产出时实时校验**（不仅 archive）。早卡早省返工 token。

### ④ tasks.md 与 plan.md 两份？建议：合并为单一 plan.md
**建议只保留 `plan.md`**，把 spec-kit 的 task 生成结果写进 plan.md，不另起 tasks.md。

理由：
- 你要"核心产物够喂饱技能就行"且吐槽"superpowers 产出太多"。writing-plans 的任务拆解与 spec-kit 的 tasks 是**同一份底层数据**，拆两份正是你反对的"产出过多"。
- 两文件引入同步成本（改一处另一处过期），正是要砍的维护债。
- 可**复用 spec-kit 的 task 生成能力**（用它产任务），但**写入 plan.md 单一产物**。能力复用，产物统一。

plan.md 结构（够喂饱五技能）：
```
# 目标（1 行）
## 任务（可勾选，每任务自洽到能直接派子代理）
- [ ] T1 id=... 场景=spec.md#S1 验收=... 依赖=无 test-first=否
- [ ] T2 ...
## 完成定义
```
- writing-plans → 产出 plan.md（瘦）
- using-git-worktrees → 按 plan.md 任务开 worktree
- subagent-driven-development → 每任务派子代理读 plan.md 该任务 + spec.md 场景 + 惰性指针
- executing-plans → 按依赖序编排
- test-driven-development → test-first 任务先写测试

**何时两份才合理**：超大/复杂特性，plan 设计理由须与"勾选跟踪"分开存档时。常规特性一律合并。

### ⑤ 在原来三套流程里，它属于什么？
桥接插件不是"塞进"某一套，而是**门面/编排层**，把自身 5 阶段映射到每套框架原生阶段：

| 我们的阶段 | spec-kit 原生 | OpenSpec 原生 | superpowers 原生 |
|---|---|---|---|
| constitution | constitution（首步） | — | — |
| specify | specify（spec.md） | propose | brainstorming*（可选）* |
| plan | tasks / plan | — | writing-plans |
| build | implement | apply | TDD + subagent + executing |
| archive | — | archive（delta 合并） | finishing-a-branch |

即：spec-kit 流水线被借用 constitution+spec+tasks；OpenSpec 借用 propose+archive；superpowers 借用 writing-plans+TDD+subagent+executing；由门面按序串联。它"属于"每套流程的**编排者**位置，不替代任何一套核心引擎。

### ⑥ 编码规范 = 代码结构符合项目结构，不能无中生有
**关键纠正**：v4 把编码规范理解成通用 linter 清单（命名/错误处理/安全）。你要的是更精准也更有价值的约束——**生成代码的结构必须贴合现有项目结构，禁止无中生有**（不得新建不符既有的目录/模块/架构模式）。

因此 constitution（第一步）的核心强制内容：
- **项目结构地图**：现有目录布局、模块边界、既有命名约定的真实快照
- **硬规则**：新文件必须落既有位置；不新建顶层目录；不引入既有项目没有的架构/模式；确需新增结构须显式申请
- 校验点：子代理产出实时卡 + archive 前卡（见③）

这条比通用规范更贴合"AI 生成代码乱建目录"的真实痛点，也更省 token（不需泛泛规范集，只卡结构一致性）。

---

## 二、修订后总览

流程：**constitution（spec-kit，强制结构基线）→ specify（OpenSpec 场景）→ plan（合并 plan.md）→ build（superpowers 五技能 + 实时结构校验）→ archive（OpenSpec delta）**

核心产物（3 + archive）：
- `constitution.md`（spec-kit，第一步，强制）
- `spec.md`（OpenSpec 场景）
- `plan.md`（writing-plans 瘦 + spec-kit tasks 合并）
- archive diff（OpenSpec）

强制门禁：constitution 定义的结构一致性，build 实时卡 + archive 卡。

---

## 三、与 v4 的差异

| 项 | v4 | v5 |
|---|---|---|
| 第一步 | 无 constitution | **constitution（spec-kit）为首步** |
| 编码规范 | 通用 linter 清单 | **项目结构一致性（禁无中生有）** |
| 校验时机 | 待定 | **锁定实时卡** |
| plan/tasks | 待定 | **合并为单一 plan.md（建议）** |
| 原生流程定位 | 未说明 | **给出阶段映射表** |

---

## 四、待拍板（剩余）

1. **constitution 内容来源**：插件自动扫描项目结构生成初稿，还是人工先写一版？
2. **实时卡误报处理**：子代理新建一个其实合理的目录被卡，是打回还是转人工确认？
3. **是否落地最小骨架**：用最小 CLI/脚本把 v2 的 dispatcher 降级成适配器，并接入 constitution 第一步？
