# SpecPowers 桥接插件 · 设计方案 v6.8

> 状态：设计已定稿，**策略 A 锁定**（选型依据见 `桥接选型总纲.md`），实现待启动。本文件只描述"做什么 / 为什么"，不描述"怎么写代码"。
> 演进：v2 新框架 → v3 纯桥接 → v4 加编码门禁 → v5 结构一致性门禁 → v6 constitution 交 speckit + 转人工 + 设计/实现分离 → **v6.1 build 能力池** → **v6.2 使用者决策 + 头脑风暴接入 + archive 职责澄清** → **v6.3 头脑风暴提升为编号阶段 1 + brief.md 结构化收口** → **v6.4 brief.md 必然产出 + 自动读取契约** → **v6.5 结构门禁零配置化 + archive 合体后校验** → **v6.6 优化模式 + archive 校验自动判断 + 削减概念暴露面 + 砍直跑模式** → **v6.7 评审驱动的质量兜底** → **v6.8 交付模型锁定为 skill 插件 + slash 命令调用（/specpowers.*），CLI 退居内部薄壳**。
> v6.7 变更摘要（评审驱动，10 项）：① 明确优化模式下选 brainstorm → brief.md 消费者为 build 上下文 + audit（§2.1.1）；② 优化模式 archive 在 OpenSpec 落 stub 入口，**两套体系入口统一**（§2.2）；③ 优化模式支持**内建回退**：build 中途可声明"升级为完整流程"，回退到 specify（§4.2.1）；④ 优化模式要求**最小验收清单** 3-5 条，替代 spec.md 作 build 验收锚点（§4.2.2）；⑤ 新增显式入口 `--fast`，使用者可直接触发优化模式（§4.2）；⑥ 优化模式确认交互改为**结构化理由**（§4.2）；⑦ 基线刷新阈值从固定 50 commit 改为**相对阈值**（30 天或项目月均 2 倍）（§3.2.1）；⑧ **brief.md 最低信息密度恢复为"至少两段"**（目标 + 至少一段其他）（§2.1.1.1）；⑨ 优化模式 archive 记录**改动范围说明**（§2.2）；⑩ 事后抽样加**"每 20 次 build 至少审 1 次"下限**（§3.2.3）；⑩① **constitution 注入策略统一**（惰性指针，两模式读取策略一致）（§4.2）。

> v6.8 变更摘要（实现方案回写，交付模型调整，7 项）：① 交付模型锁定——最终交付物为全新 **skill 插件**，经 WorkBuddy 以 `/specpowers.*` slash 命令加载调用，不再是对外 `specpowers` 二进制 CLI（§0.1、§4.2）；② 调用命名空间统一为 `specpowers.<动作>`，对齐 `/speckit.constitution` 框架级命名惯例，避免与 superpowers 裸名技能（brainstorming 等）冲突；③ 各阶段为独立显式 slash 命令（`.constitution`/`.brainstorm`/`.specify`/`.fast`/`.plan`/`.build`/`.archive`/`.baseline`/`.reset`/`.status`），命令名即阶段名，无"无参自动推进"魔法；④ 内建回退（升级为完整流程）降级为**运行时自然语言交互**，不再暴露为 `specpowers upgrade` 命令；⑤ 新增调试命令 `/specpowers.status`（打印 stage/mode/fallback_used），不计入概念暴露面；⑥ 仅保留两个带 flag 命令：constitution `--force`、archive `--force-merge-check`；⑦ `bin/specpowers` 退居 skill 内部薄壳（仅 agent 经 Bash 触发 `bridge/`），对外不可见。

---

## 0. 一句话定位

**SpecPowers 是一个薄桥接插件（Facade + Adapter + Dispatcher），不重写任何一套框架的逻辑，只复用 OpenSpec / spec-kit / superpowers 的原生能力，并补上两条它们都没有的硬约束：结构一致性门禁、实时卡转人工。**

### 0.1 用户速查（使用者只需懂这些）

> 本节是面向使用者的最小认知集。以下 §1~§9 为设计内幕，使用者无需通读，需要查机制细节时再翻。

**3 个核心概念**：
1. **流水线**：`constitution → brainstorm(可选) → specify → plan → build → archive`（小特性 agent 提示走优化模式，使用者确认后跳过 specify/plan）
2. **结构门禁**：build 时实时卡代码是否契合项目结构（通过 / 转人工 / 打回），archive 合并后再卡一次（自动判断多分支开/单分支关）
3. **能力声明**：build 阶段开哪些能力（worktree / subagent / TDD）由使用者声明，框架不自适应

**2 个开关**（使用者可决定）：
| 开关 | 选项 | 何时选 |
|---|---|---|
| 跑 brainstorm？ | 跑 / 不跑 | 需求模糊或高风险才跑 |
| build 启用哪些能力？ | executing(默认) + worktree/subagent/TDD | 按需声明 |

> archive 合体后校验已改为 **archive 阶段自动判断**（多分支开、单分支关），使用者无需声明。

**产物清单**（使用者会看到的文件）：
- `constitution.md`（项目级，一次）
- `brief.md`（跑了 brainstorm 才有，必然产出；优化模式下可选跑 brainstorm）
- `spec.md`（specify 产出；优化模式跳过）
- `plan.md`（plan 产出；优化模式跳过）
- 代码（build 产出）
- archive 记录（archive 产出；优化模式：git commit + OpenSpec stub 入口 + 改动范围说明，无 spec delta）

**对使用者可见的内部机制**：
- **优化模式**：agent 读需求判断是否为小特性，是则提示使用者确认（一并问能力声明 + 是否跑 brainstorm），确认后走优化模式（跳过 specify/plan，直接 build + archive）；其他情况默认走完整流程。**也可显式 `/specpowers.fast` 直接进优化模式确认**。详见 §4.2。
- **内建回退**：优化模式 build 中途可声明「升级为完整流程」，回退到 specify 阶段，已写代码保留作参考。详见 §4.2.1。

**命令速查（slash 命令，经 WorkBuddy skill 插件调用）**：`/specpowers.constitution [--force]`（项目原则）· `/specpowers.brainstorm "<需求>"` · `/specpowers.specify "<需求>"` · `/specpowers.fast "<需求>"`（优化模式入口）· `/specpowers.plan` · `/specpowers.build` · `/specpowers.archive [--force-merge-check]`（收尾）· `/specpowers.baseline`（基线刷新）· `/specpowers.reset`（故障恢复）· `/specpowers.status`（调试）。命令名即阶段名，无"无参自动推进"魔法；命名空间 `specpowers.` 对齐 `/speckit.constitution` 框架级技能命名惯例。

---

## 1. 核心原则（锁定）

| 原则 | 说明 | 来源 |
|---|---|---|
| 桥接不重写 | 三套框架原生引擎原封不动，插件只做共享契约 + 适配器翻译 + 按需路由 | 用户决策③ |
| 唯一硬门禁 = 结构一致性 | 生成代码必须契合现有项目结构，禁止无中生有 | 用户 v5-⑥ 澄清 |
| 原则层交 speckit | constitution 由 `/speckit.constitution` 生成，聚焦质量/测试/UX/性能 | 用户本回合① |
| 实时卡 + 转人工 | build 产出即时校验；拿不准则挂起转人工，不自动放行/打回 | 用户本回合② |
| 产物价值闸门 | 每个产物必须有下游消费者，驱动不了任何东西的文档一律不产 | 用户 v4-② |
| TDD 可选；头脑风暴为编号可选阶段（高权重） | 强制"代码结构怎么写"；TDD 不强制；头脑风暴提升为特性级流水线**阶段 1**（可选但编号在册，权重高） | 用户 v3 + 本回合 |
| 设计先于实现 | 先定方案，用户拍板后再落地 | 用户本回合③ + 跨项目偏好 |
| **零配置开箱即用** | 结构门禁不外置规则文件，判据源不依赖任何框架内部模型；用户无需配置即可使用 | 用户 v6.5 拍板 |
| **优化模式**（v6.6→v6.7） | agent 判断是否为小特性，是则提示使用者确认后跳过 specify + plan（brainstorm 可选），直接 build + archive；其他默认走完整流程；v6.7 新增 `--fast` 显式入口 + 内建回退 + 最小验收清单 | 用户 v6.6 拍板 |
| **削减概念暴露面**（v6.6） | 对外只暴露 3 概念 + 2 开关；Facade/Adapter/Dispatcher/git_ref/惰性指针/价值闸门/自动读取契约作为设计内幕（优化模式对使用者可见但无需声明开关） | 用户 v6.6 拍板 |

---

## 2. 流程（5 阶段，门面编排）

```
项目级（一次性）: constitution(speckit)
特性级流水线（常规）:
  1. brainstorm ──▶ 2. specify ──▶ 3. plan ──▶ 4. build ──▶ 5. archive
     (superpowers     (OpenSpec)    (superpowers     (superpowers     (OpenSpec
      brainstorming)                writing-plans   能力池+实时门禁)  delta合并
                                 +spec-kit tasks)                   +合体后校验)
特性级流水线（优化模式，v6.6→v6.7，agent 判小 + 使用者确认 / `--fast` 显式触发）:
  brainstorm(可选) ──▶ 4. build(含最小验收清单) ──▶ 5. archive(git commit + OpenSpec stub + 自定义记录)
  注：跳过 specify + plan；build 仍受实时门禁；archive 不做 spec delta（无 spec 源），但在 OpenSpec 落一条 stub 入口保持可追溯性；合体后校验优化模式默认关（无合并），常规模式自动判断。
  注：constitution 注入策略统一为惰性指针（与常规模式一致，§4.2）。
  注：agent 读需求判断是否为小特性，判小则提示使用者确认（含结构化理由），其他默认走完整流程；使用者也可 `--fast` 显式触发。
  注：brainstorm 在优化模式下可选——跑则 brief.md 消费者为 build 上下文 + audit（§2.1.1 明确）。
  注：build 中途支持内建回退——使用者可声明"升级为完整流程"切回 specify/plan（§4.2.1）。
注：brainstorm 为编号阶段 1，但可选（使用者决定跑不跑）。
```

| 层级 | 阶段 | 借用的原生能力 | 桥接层职责 | 主产物 |
|---|---|---|---|---|
| 项目级（一次性） | constitution | spec-kit `/speckit.constitution` | 触发生成；扫描项目结构出**结构基线快照**（带 `git_ref`） | `constitution.md` + 结构基线（自动） |
| 特性级 | **1. brainstorm** | superpowers `brainstorming` | 触发探索；产物权衡（§2.1.1） | `brief.md`（必然产出） |
| 特性级 | **2. specify** | OpenSpec 场景格式（`#### WHEN/THEN`） | 适配器把需求翻译成 OpenSpec 场景 | `spec.md` |
| 特性级 | **3. plan** | superpowers `writing-plans`（瘦身）+ spec-kit task 生成 | 合并为单一 `plan.md`；每任务带 id/场景指针/验收/依赖/test-first 标记 | `plan.md` |
| 特性级 | **4. build** | superpowers **能力池**（由使用者显式决定启用） | **实时结构门禁**（pass / 转人工 / reject）+ 按声明路由 | 代码（落 worktree） |
| 特性级 | **5. archive** | OpenSpec archive（delta 合并） | 复用 delta 合并 + **桥接四职责**（§2.2，含合体后校验，自动判断） | archive 记录 |
| 特性级（v6.7） | **优化模式** | superpowers build | 跳过 specify/plan；build 实时门禁不跳；archive 绕过 OpenSpec 原生命令改用 git commit + OpenSpec stub；**build 含最小验收清单 3-5 条**；**支持内建回退切完整流程** | 代码 + archive 记录 + OpenSpec stub |

---

### 2.1 头脑风暴阶段（brainstorming，可选，高权重）

- **提升为编号阶段**：原设计把头脑风暴画成虚线支路，结构上报低了它的权重。superpowers 将其作为 spec 前的核心探索环节，且用户明确其权重高——故提升为特性级流水线**阶段 1（可选但编号在册）**。这既给足结构地位，又保留"可选"（跑不跑由使用者决定），不违背"流程可选 / 使用者作主"。
- **位置**：constitution（项目级，一次）之后、specify 之前。constitution 讲"代码怎么写好"（与特性无关）；头脑风暴讲"这个特性要做什么"，必须先于把需求固化成 spec 场景。
- **触发权在用户**：需求已清楚就直接进 specify；觉得模糊 / 复杂 / 高风险就先跑 brainstorming 把问题"榨"清楚。

### 2.1.1 头脑风暴产物权衡（必然产出，附理由）

头脑风暴**确实产出物**，但原始产物（自由对话、试错、死胡同）与提炼产物价值天差地别。取舍点不是"有 vs 无"，而是"**原始 vs 提炼**"——提炼产物 `brief.md` 是头脑风暴的**必然产出物**：

- **原始探索过程：不持久化。** 自由对话、被推翻的设想、试错路径，是 token 黑洞且无回头读者，违反价值闸门。跑完即弃，仅在会话内临时存在。
- **提炼产物 `brief.md`：必然持久化（只要跑了 brainstorming，就落盘）。** 仅保留对后续有真实价值的部分——**目标 / 关键决策 + 理由 / 被拒方案 / 已决开放问题**。这是"为什么这么定、放弃了什么"的记录，`spec.md`（场景）不承载这部分。**不做复杂度判断**——既然使用者决定投入一轮头脑风暴，决策脉络就值得留底；成本只是一份短结构化文件，不是逐字转录，远比"事后想不起当初为什么这么定"便宜。
- **下游消费者（满足价值闸门）**：`brief.md` 是 `specify` 阶段的**自动输入**，喂给 OpenSpec 场景生成 `spec.md`；同时也是审计 / 交接的凭证。正因为有消费者、可追溯，它才是必然产出而非可选副产物。
- **与 `spec.md` 职责不混**：`brief.md` = 为什么 / 被拒方案（决策脉络）；`spec.md` = 做什么（验收场景）。两者互补不重叠。

**优化模式下 brief.md 的消费者（v6.7 明确）**：优化模式跳过了 specify，但若使用者在确认时选跑了 brainstorm，`brief.md` 仍为必然产出物——其消费者从"specify 的自动输入"切换为两个消费路径，仍满足价值闸门：
1. **build 阶段子代理上下文**：子代理读取 brief.md 的决策脉络（目标/决策+理由/被拒方案），理解"为什么要这么改"，作为验收锚点的补充背景（虽不如 WHEN/THEN 精确，但比纯需求描述 + 对话强）；
2. **archive 审计 / 交接**：作为决策追溯凭证（与常规模式一致）。

这两个消费者均为"功能性消费"而非心理安慰——子代理缺了决策脉络会更频繁地回头问使用者"为什么这么定"，而审计缺了决策记录就变成了考古。因此优化模式下选 brainstorm 产 brief.md **不违反价值闸门**。

**取舍一句话**：取舍在原始 vs 提炼，非全留全砍；brief.md 有下游消费者（specify）+ 审计价值，必然落盘；必然产出规则简单、行为可预期，使用者无需判断"要不要留"。

##### 2.1.1.1 提炼的机器可执行规则（方案 1：结构化收口）

上版只定义了 brief.md"留哪四类"，没定义"怎么从对话里识别这四类"。本版采用**方案 1**：把提炼动作**前移到 brainstorming 的收尾契约**，而非桥接层事后二次猜测。

- **收口契约（brainstorming 结束必做）**：brainstorming 收尾必须输出一份**结构化决策摘要**，并**必然落盘为 `brief.md`**（这是头脑风暴的必然产出物）。**至少含"目标"段 + 至少一段其他**——目标段必有，其余三段按需填写但不可全空（至少填一段，保证最低信息密度，避免退化为一句话占位）。格式参考（有则填，无则略）：
  ```markdown
  ## 目标
  - <一句话说清这个特性要达成什么>（必有）

  ## 关键决策（若有）
  - 决策：<定了的方案>
    理由：<为什么这么定，而非别的>

  ## 被拒方案（若有）
  - 方案：<考虑过但放弃的>
    放弃理由：<为什么不行 / 不划算>

  ## 已决开放问题（若有）
  - 问题：<暂定不下来、但已记录结论的>
    当前结论：<先按 X 处理，后续 Y 再调>
  ```
- **提取判据（成型信号，非事后猜）**：每个字段对应明确信号，agent 按信号填，不靠自由发挥——
  - 目标 ← "我们要做…" / "目的是…"
  - 关键决策 ← "决定用…因为…" / "就按…来"
  - 被拒方案 ← "不采用…理由是…" / "否决了…因为…"
  - 已决开放问题 ← "这个先按…后面再看" / "阈值暂定…上线调"
- **brief.md 来源 = 收口产物直接落盘**：满足持久化条件（§2.1.1 第三点）时，`brief.md` 内容即这份结构化摘要，桥接层**不做二次抽取**；不满足则不写文件，仅会话内临时存在。
- **为什么方案 1 优于事后抽取**：提炼变成 brainstorming 的收口契约（产出时即结构化），而非桥接层读散对话二次猜测——准确率稳、不费额外 token、跨 agent 一致。

### 2.2 archive 在桥接层的职责
archive 阶段复用 OpenSpec 原生的 delta 合并（只合并 diff，不重写整篇，省 token；优化模式绕过，见 §4.2）。桥接层在此额外承担**四件事**（v6.5 新增第 4 项合体后校验，**v6.6 改为 archive 阶段自动判断**），且 build 实时门禁 + archive 合体后校验**互补**：
1. **清账转人工挂起**：build 阶段"转人工"的项若仍未确认，archive 前必须全部了结（通过 / 调整），不允许带未决挂起归档。
2. **constitution 原则证据核查**：轻量核对产出是否沾边 constitution 的质量/测试/UX/性能原则（不重验，只查证据是否存在，如测试是否执行、是否有明显性能回归），作为收尾而非重闸门。
3. **收尾产物**：标记 `plan.md` 任务完成（优化模式豁免，无 plan.md）、产出 archive 记录（供后续变更追溯）。
4. **合体后结构校验**（v6.5 新增，v6.6 改为自动判断）：delta 合并后**加一次"合体后"结构校验**，抓 build 阶段单点视角漏掉的合并后违规（如 A 加了 `utils/v2/`、B 也加了 `utils/v2/` 不同实现，合并后冲突）。校验方式：复用 build 阶段的基线快照 + 三态判定逻辑，对合并后的代码再跑一次。**archive 阶段自动判断**——检测到多分支 delta 合并则开，单分支自动关（见 §3.2.4 详述）。

- **archive 阶段只转人工、不打回**：archive 是收尾阶段，打回意味着回头改代码、破坏收尾语义；触发问题一律转人工挂起，由人工决定是回头改还是接受。
- **桥接层不做的事**：不在 archive 重新全量扫描结构一致性（合体后校验是增量校验，非全量重扫）；不重新生成整套 spec。
- **优化模式 archive 仍做原则核查**：优化模式跳过合体后校验，但 constitution 原则证据核查（第2件事）仍做——轻量且有价值，不豁免。
- **优化模式 archive 入口统一（v6.7）**：优化模式的 archive 绕过 OpenSpec 原生命令（无 spec delta），但在 OpenSpec archive 目录下**落一条 stub 记录**——内容仅为「优化模式，详见 git commit <hash>」+ 改动范围说明，保持所有特性的 archive 入口统一可查，避免两套体系分裂。

---

## 3. 两条硬约束（插件独有，框架本身没有）

### 3.1 constitution：交给 speckit，聚焦四类原则
- **生成方式**：执行 `/speckit.constitution`，由 spec-kit 产出 `constitution.md`。
- **强制范围**（用户指定）：
  1. **代码质量**（命名、模块化、错误处理、无死代码）
  2. **测试标准**（覆盖率阈值、何时 TDD、done 的定义）
  3. **用户体验一致性**（设计系统、无障碍）
  4. **性能要求**（延迟预算、避免 N+1、资源占用）
- **注入方式**：沿用 v3 惰性指针——`constitution.md` 是约 20 行清单，传的是**文件路径指针**而非整段内容；子代理只在"真会改变行为"时读取。不用 `<context>` 全量内联。
- **注意**：constitution **不**承载结构规则（那是 3.2 的事），保持 spec-kit 原生职责纯粹。

### 3.2 结构一致性门禁（桥接层内置，实时执行，零配置）

**设计原则**：开箱即用，零配置。判据不外置为规则文件，判据源不依赖任何框架内部模型。结构门禁留在桥接层（接受与三套框架升级的维护成本，换取桥接层能力完整）。

#### 3.2.1 基线快照（constitution 阶段一次扫死，零配置）

**只读三个通用源**：

| 源 | 用途 |
|---|---|
| `git ls-tree HEAD` | 顶层目录列表 |
| 依赖清单（自动识别 `package.json` / `pom.xml` / `requirements.txt` / `go.mod` / `Cargo.toml` 等） | 依赖列表 |
| `src/` 下二级目录命名规律 | 架构模式列表 |

**生成时机**：constitution 阶段一次性扫描生成，**不增量刷新**。理由：constitution 是顶层结构文件，顶层结构演进频率低；接受基线过期的误杀风险，换取流程简单。

**刷新触发条件（v6.6 明确，缓解长期项目基线严重过期）**：
- **自动触发**：`constitution.md` 被修改时自动重扫基线（constitution 本身是顶层结构文件，改它意味着结构可能变）
- **手动触发**：使用者执行 `/specpowers.baseline` 显式刷新
- **提示触发**：build 阶段 agent 读 `git_ref` 发现基线过期时提示使用者"基线过期，建议刷新；本次仍按旧基线判，拿不准转人工"。**触发阈值（v6.7 改为相对）**：满足以下任一条件则提示——① 基线距今 > 30 天；② 基线距 HEAD 的 commit 数 > 项目月均 commit 数 × 2（自动估算，全新仓库无历史数据时兜底用 30 天阈值，不触发条件②）。相对阈值避免了固定 50 commit 在不同项目上"一天就触发 vs 半年不触发"的极端分化。
- 不破坏"一次扫死"的简单性——只是给刷新入口，不强制增量刷新

**输出**：内部 JSON，**不暴露给用户、不需要用户配置**，带 `git_ref` 字段标记基线对应的 commit。

**`git_ref` 的作用**（v6.5 缓解基线时效性）：agent 在 build 阶段判定时，可读取基线的 `git_ref` 知道自己在比对哪个 commit。若发现基线已明显落后于当前 main（如期间合入了新的架构层），agent 可主动判断"基线旧了，我转人工"——这不破坏"一次扫死"的简单性，只是给 agent 一个额外信号。

#### 3.2.2 执行位置

build 阶段、子代理每次产出即校验（实时，非 archive 才卡）。

#### 3.2.3 三态结果（agent 主观判断）

判据由 agent 主观判断，参考三类信号：新建基线里没有的顶层目录 / 引入基线里没有的依赖 / 在基线既有模式外新建架构层。

- ✅ **通过**：agent 判定契合基线 → 进入 executing 编排
- 🔄 **转人工**：agent 拿不准（如新目录可能合理也可能越界）→ 挂起，推送人工确认，不自动决策
- ❌ **打回**：agent 判定明显违规（随机顶层目录、引入无关框架）→ 记录原因，子代理重写

挂起/打回消息必须包含：触发信号 + 具体证据，不需要用户查文档。

**为什么维持 agent 主观判断（不引入客观化清单）**：客观化清单（如"新增目录一律转人工"）虽可预测、可测试，但增加配置门槛与复杂度，违背零配置开箱即用原则。主观判断接受跨 agent 不可重复、无法回归测试的代价，换取零配置与灵活性——agent 可结合项目上下文做更细腻的判断，而非机械触发。

**失败模式兜底（事后抽样审计）**：主观判断有两种失败模式——一直判"通过"（放羊，门禁失效）或一直判"转人工"（保守，使用者被淹没）。兜底机制：archive 阶段**随机抽样 10% 的"通过"判定**，由人工或更高级 agent 复核；若不通过率超阈值（如 >30%），下次调 agent prompt 收紧判据。**v6.7 加下限**：为保证低频项目也不漏审——增加绝对下限 **"每 20 次 build 至少抽审 1 次"**，与 10% 抽样率取 max。这是软兜底，不破坏零配置，也不增加使用者负担。

#### 3.2.4 archive 阶段合体后校验（v6.6 改为自动判断）

archive 阶段在 delta 合并后**加一次"合体后"结构校验**，抓 build 阶段单点视角漏掉的合并后违规（如 A 加了 `utils/v2/`、B 也加了 `utils/v2/` 不同实现，合并后冲突）。

**校验方式**：复用 build 阶段的基线快照 + 三态判定逻辑，对合并后的代码再跑一次。

**v6.6 调整：archive 阶段自动判断是否开合体后校验，使用者无需声明**。理由：
- build 实时门禁抓"单点违规"（早卡早省返工），archive 合体后校验抓"合并后违规"（单点视角抓不到的增量）——**二者互补，非冗余**。
- 但单分支场景下 archive 校验确属冗余（无合并即无合并后违规）；多分支并行合并时才有价值——**故由 archive 阶段自动检测**：
  - 检测到多分支 delta 合并 → **自动开**
  - 单分支（无合并）→ **自动关**
- 使用者无需在事前声明"单分支还是多分支"——archive 时才知道有没有合并，所以时机也定在 archive 阶段自动判断，使用者零负担。
- **检测方式**：archive 阶段检查 git log 自上次 archive 以来是否有 merge commit；有 merge commit → 多分支 → 开；无 → 单分支 → 关。
- **工作流局限（v6.6 明确）**：merge commit 检测对 rebase / squash merge / cherry-pick 工作流会漏判（这些工作流无 merge commit 但代码实际来自多分支）。处理方式：
  - **自动检测漏判时**：archive 阶段同时检查"自上次 archive 以来是否有多个作者提交"作为补充信号——有多作者 → 可能多分支 → 开
  - **手动兜底**：使用者可显式 `/specpowers.archive --force-merge-check` 强制开合体后校验
  - **文档标注**：rebase/squash 工作流项目建议在 archive 时观察是否有多作者提交，或直接手动触发
- **优化模式默认关**：优化模式不走多分支合并（archive 绕过 OpenSpec），无合并后违规场景。
- **不实时卡的代价**（论证 build 实时门禁为何不能去）：若去掉 build 实时门禁只在 archive 卡，代码已写一大坨才发现违规 → 返工成本爆炸，"早卡早省"核心价值丢失，门禁形同事后告状。实时门禁是"早卡"，archive 校验是"补漏"，二者缺一不可但可分别开关。

**archive 阶段只转人工、不打回**：archive 是收尾阶段，打回意味着回头改代码、破坏收尾语义；触发问题一律转人工挂起，由人工决定是回头改还是接受。

#### 3.2.5 弱耦合保证

| 升级场景 | 影响 |
|---|---|
| OpenSpec / spec-kit / superpowers 升级 | 不影响（门禁不读任何框架内部模型） |
| 跨项目复用 | 引擎一套代码跑多项目，基线自动扫 |
| 框架语义级违规 | 抓不住——本就该 spec-kit 自己管，不在桥接层职责内 |

#### 3.2.6 不做的事

- 不外置规则文件（不为 5% 项目特定场景给 95% 用户加配置门槛）
- 不读任何框架的内部 AST / 模型（保证弱耦合）
- 基线不强制增量刷新（接受误杀换简单）；有刷新入口见 §3.2.1（自动/手动/提示三路）

---

## 4. superpowers 五技能桥接（writing-plans 瘦身）

用户要求五个能力全要，但 `writing-plans` 必须瘦身到"够喂饱其余四个"。

**关键修正（v6.1 → v6.2）**：build 阶段**不是固定调用四个技能**，而是从一个**能力池**中由**使用者显式决定**启用哪些。`writing-plans` 出现在 plan 阶段；其余四个（worktrees / subagent / executing / TDD）是 build 阶段的**候选能力**。**"按需"的主语是使用者，不是框架**——框架不根据任务规模自动判断开不开某个能力，而是如实执行使用者本轮的声明（运行级开关，或 `plan.md` 中由使用者填的能力标记）。这与"原则强制、流程可选、使用者作主"的总原则一致。

| 技能 | 阶段 | 默认 / 使用者决定 | 启用条件 |
|---|---|---|---|
| `writing-plans`（瘦身） | plan | 默认 | 每轮都跑，产出 `plan.md` |
| `executing-plans` | build | **默认（编排者 / conductor）** | build 阶段总指挥；优化模式下使用者可在确认时一并决定是否跳过编排由主控直接做 |
| `using-git-worktrees` | build | **使用者显式启用** | 仅在常规模式 plan.md 声明——优化模式禁止开（依赖 plan.md 分派，跳过 plan 无任务可派） |
| `subagent-driven-development` | build | **使用者显式启用** | 仅在常规模式 plan.md 声明——优化模式禁止开（小特性 + 隔离上下文是矛盾组合） |
| `test-driven-development` | build | **使用者显式启用** | 在 plan.md（常规）或确认时问答（优化模式）声明 `test-first` |

### 4.1 build 阶段能力由使用者决定（非规模自适应）
- **"运行级开关"的定义**：指插件启动时的**交互式问答 / 命令行参数**，非配置文件。常规流程下使用者在 `plan.md` 任务上标记能力声明；优化模式下因跳过 plan，能力声明改为在确认优化模式时的交互问答采集。
- **决策权在谁**：每个 build 能力是否启用，由**使用者**显式声明（plan.md 标记 / 确认时问答）。框架**不**替使用者按规模自适应拍板；`executing-plans` 默认 conductor，可被使用者覆盖；优化模式禁止开 worktree/subagent（见 §4.2）。
- **Dispatcher 的角色**：如实执行使用者的声明——把启用的工作流 / 子代理 / 测试纪律接上，不自行增删能力。**结论**：能力池的"按需"= 使用者按需，每轮 build 启用集合由使用者决定。

**为什么 tasks.md 合并进 plan.md**：二者底层是同一份数据，拆两份正是 superpowers"产出过多"的槽点。复用 spec-kit task 生成能力，但只写 `plan.md` 一个产物；仅当超大特性需把"设计理由"与"勾选跟踪"分离时才值得两份。

### 4.2 优化模式（v6.6→v6.7，跳过 specify/plan，agent 判小后提示确认 / `--fast` 显式触发）

**解决什么**：避免"改 2 行 bug 也要走完 specify + plan 完整产物"的过重流程。优化模式**真正跳过 specify + plan**，直接进 build + archive；agent 判断是否为小特性，是则提示使用者确认，其他情况默认走完整流程。

**适用场景**：bugfix / 极小改动 / 不引入新场景的调整。

**两种触发方式（v6.7 新增显式入口）**：
1. **自动判断**：插件启动时 agent 读需求判断是否为小特性，判小则提示使用者确认
2. **显式 `/specpowers.fast`**：使用者主动声明 `/specpowers.fast "<需求>"`，跳过 agent 判断，直接进优化模式确认环节——适用于使用者明确知道是小改动的场景，避免 agent 误判带来的摩擦

**触发机制（agent 判断是否小特性，是则提示确认，其他默认完整流程）**：
- **时机**：插件启动时，agent 读取使用者输入的需求描述，判断是否为小特性
- **判据**（agent 参考，非硬阈值，聚焦需求性质而非结果指标）：
  - bugfix / 纯配置 / 纯文案 / 纯重构
  - 不新增 spec 场景
  - 单文件、改动范围可预期
  - 注：不判"改动行数"——agent 在 build 前无法准确预估行数，预判不准；改以需求性质判断
- **行为**：
  - **判为小特性** → 给使用者提示并一并问能力声明：
    ```
    「判为小特性 [理由：<bugfix/单文件/无新场景/纯配置/纯重构>，置信度：<高/中>]
      建议走优化模式（跳过 spec/plan，直接 build）。
      确认走优化模式？[Y/n]
      是否先跑 brainstorm？[默认：不跑]
      （若确认）需要开 TDD 吗？[默认：不开，仅 executing]」
    ```
    - 使用者确认 → 走优化模式（能力声明在此时采集，替代原 plan.md 声明）
    - 使用者否决 → 走完整流程
  - **结构化理由规则（v6.7）**：agent 判小特性的理由必须按固定格式输出——`[理由：<信号1>/<信号2>/...，置信度：<高/中/低>]`——信号集合为 {bugfix, 单文件, 无新场景, 纯配置, 纯文案, 纯重构}。置信度"低"时允许提示但必须标注，供使用者决策参考。
  - **优化模式禁止开 worktree / subagent**：二者均依赖 plan.md 任务清单分派（worktree 分派子分支、subagent 分派任务），优化模式跳过 plan 无任务可派；且小特性 + 隔离上下文是矛盾组合——worktree/subagent 的核心价值在隔离大特性上下文，建分支/开子代理的固定开销本身就超过了小特性的工作量。确认时只问 TDD + brainstorm，不问 worktree/subagent。
  - **不判为小特性**（含大特性、拿不准的边界）→ **默认走完整流程，不提示**
- **与结构门禁一致**：跟 §3.2.3 实时门禁一样，采用 agent 主观判断而非客观化清单，保持零配置原则统一
- **不可由 commit message 触发**：commit 发生在 build 之后，时序对不上；只由插件启动时的需求判断触发

**跳过的阶段与产物**：
- 跳过 specify → 不产 `spec.md`
- 跳过 plan → 不产 `plan.md`（能力声明前移到确认环节采集）
- brainstorm → **可选**（使用者在确认优化模式时一并决定是否先跑头脑风暴；小特性通常不需要，默认不跑；**若选跑则 brief.md 的消费者为 build 上下文 + audit**，详见 §2.1.1）
- build 仍受**实时结构门禁**约束（不能跳），且**额外要求最小验收清单 3-5 条**（见 §4.2.2）
- archive 退化为 git commit + **OpenSpec stub 入口** + 自定义 archive 记录（仅代码 delta + 改动范围说明 + 清账 + 原则核查，跳过合体后校验）

**前置条件（v6.6 明确）**：
- 优化模式是特性级流程，但依赖项目级产物——**`constitution.md` 和结构基线快照必须存在**
- 若项目第一次跑就走了优化模式（constitution 未生成），Dispatcher 拒绝开工，提示使用者先跑 constitution
- 理由：build 实时门禁需要基线快照作靶子，constitution 原则需要 constitution.md 作注入源

**archive 退化行为（v6.7 更新）**：
- 优化模式没产 `spec.md`，OpenSpec archive 原生命令强依赖 spec delta，**无法执行**——故优化模式 archive **不走 OpenSpec archive 原生命令**
- 退化处理：archive 改用 **git commit + OpenSpec stub 入口 + 自定义 archive 记录**——做代码 delta（git diff）+ **改动范围说明**（改了什么文件、影响了什么行为，3-5 句，成本极低但为后续迭代提供追溯锚） + 清账收尾 + constitution 原则核查，跳过合体后校验
- **OpenSpec stub 入口（v6.7）**：在 OpenSpec archive 目录下落一条 stub 记录，内容为「优化模式，详见 git commit `<hash>`」+ 改动范围说明（纯文本，不依赖 OpenSpec delta 机制），保持所有特性的 archive 入口统一可查，避免"部分特性在 OpenSpec archive 里、部分只在 git log 里"的分裂
- archive 记录中标记"本轮为优化模式，无 spec delta，未走 OpenSpec archive 原生命令"
- 若使用者在优化模式确认时选跑了 brainstorm，则 `brief.md` 仍归档（作为决策脉络追溯）
- **constitution 注入策略统一（v6.7）**：与常规模式一致，采用惰性指针注入——build 子代理在"真改变行为时"才读 constitution.md。不因优化模式而改变注入方式，消除两套模式下的隐式行为差异

**代价与兜底**：
- 代价：失去场景锚点（无 `spec.md` 的 WHEN/THEN），build 子代理自测验收依据变弱
- 兜底：靠最小验收清单（§4.2.2）+ 需求描述 + 确认时的对话 + build 实时门禁 + code review；不靠 spec 场景

#### 4.2.1 内建回退路径（v6.7）

**解决问题**：使用者在优化模式 build 到一半，发现「其实这个改动不小，需要 spec」，v6.6 下只能硬着头皮走完或放弃重来。v6.7 提供内建回退。

**触发方式**：使用者在 build 阶段任意时刻以**运行时自然语言**声明「升级为完整流程」（不暴露为技能命令，对应实现方案方案 A）。

**回退行为**：
1. 暂停当前 build
2. 保留已写代码在原分支（不 commit，不丢弃）
3. 回到 specify 阶段，按完整流程走 specify → plan → build → archive
4. 进入新 plan 的 build 阶段时，已写代码**作为 reference context 自动加载给子代理**（子代理可参考已有实现，但需按新 plan 的任务重新验证/调整，不当作「已完成任务」直接跳过）
5. archive 记录标记「优化模式回退，本轮为完整流程」

**约束**：
- 回退只允许一次（从优化模式切到完整流程），不可往返切换（避免状态混乱）
- 回退后跳过 brainstorm——已走过优化模式的确认环节，决策脉络在对话中已存在
- 若选跑了 brainstorm 产了 brief.md，回退后 brief.md 作为 specify 的自动输入（回到常规消费链路）
- `/specpowers.fast` 显式触发的优化模式同样支持回退——`/specpowers.fast` 只是跳过了 agent 判断环节，不影响回退路径的可用性

#### 4.2.2 最小验收清单（v6.7）

**解决问题**：优化模式没有 `spec.md` 的 WHEN/THEN 场景，build 子代理缺少结构化的验收锚点——仅靠需求描述 + 对话会让子代理在"做完了吗"这个问题上反复回头问使用者。v6.7 引入最小验收清单（3-5 条），不重、但够用。

**格式**：在 build 阶段启动时，Dispatcher 基于需求描述 + 确认对话自动生成 3-5 条验收项（checklist），格式：
```markdown
## 验收清单（优化模式）
- [ ] <验收项 1>
- [ ] <验收项 2>
- [ ] <验收项 3>
- ...
```
不接受少于 3 条的清单（太少无意义），上限 5 条。**超过 5 条时，系统提示「验收清单过长，可能不是小特性，建议升级为完整流程」**——使用者可选择接受建议（触发回退，见 §4.2.1）或手动缩减清单至 5 条以内继续优化模式。

**产出与消费**：
- 不落独立文件，作为 build 子代理的内部上下文（同对话内），archive 时写入 stub 记录
- 子代理以清单为验收锚点逐条自测，完成后勾选
- archive 清账时检查清单是否全部勾选，未全勾选 → 转人工

**与 spec.md 的关系**：验收清单不是"缩水版 spec.md"，不要求 WHEN/THEN 格式，只是「改了什么 → 应该看到什么」的最简对照。它是 build 子代理的"自测指导"，不承担 spec.md 的场景覆盖、回归保护等职责。

**为什么"只判小、其他默认完整流程"是对的**：简单（只判一个二值状态）+ 默认安全（拿不准走完整流程）+ 提示最小化（只在判小时提示）+ 使用者保留确认权 + 可追溯（archive 记录标记）。

**（v6.6）砍掉直跑模式的理由**：直跑模式被优化模式取代——小特性走优化模式、其他走完整流程，二选一无中间态，规则最简。直跑作为"中间态"存在价值弱（精简版省的 token 有限，但多一个模式多一份认知负担 + 实现成本）。

---

## 5. 核心产物清单（价值闸门后）

| 产物 | 下游消费者 | 是否必须 |
|---|---|---|
| `constitution.md` | build 阶段子代理（惰性指针）、archive 核查 | 必须 |
| `brief.md` | `specify` 阶段自动输入（喂 spec.md）；审计 / 交接；优化模式下作为 build 上下文 | **必须（只要跑了 brainstorm 即为必然产出物）**；优化模式下 brainstorm 可选，故 brief.md 亦可选 |
| `spec.md` | plan 阶段任务拆解的"场景指针"来源 | 必须（优化模式豁免，由验收清单替代） |
| `plan.md` | build 阶段 worktree / subagent / executing 的输入 | 必须（优化模式豁免） |
| 验收清单（优化模式） | build 子代理自测验收锚点 | 优化模式必须（3-5 条，不落独立文件，archive 写入 stub） |
| 结构基线快照（带 `git_ref`） | build 阶段实时门禁比对；archive 合体后校验（自动判断） | 自动，非交付物 |
| archive 记录 | 后续变更追溯 | 必须 |
| OpenSpec stub（优化模式） | 保持 archive 入口统一可查 | 优化模式必须（薄记录：模式标记 + git commit hash + 改动范围说明，改动范围说明为 stub 的一部分，不独立成文件） |

### 5.1 产出物消费关系（含原因）

按流水线列出每个产物的"谁产出 → 谁消费 → 为什么这么连"。消费边分两类：**下游消费**（下一阶段驱动流程）与**跨阶段只读**（原则 / 基线被多处引用）。

| 产物 | 生产者 | 消费者 | 消费内容 | 为什么这么连（原因） |
|---|---|---|---|---|
| `constitution.md` | constitution 阶段（spec-kit） | build 子代理（惰性指针）；archive 核查 | 质量 / 测试 / UX / 性能原则 | 原则是跨阶段常量，产一次、多处只读指针；不内联全量 → 省 token 且避免漂移 |
| 结构基线快照（带 `git_ref`） | constitution 阶段（插件自动扫描） | build 实时结构门禁；archive 合体后校验（自动判断） | 目录树 + 既有依赖 / 模式清单 + 基线 commit | 门禁的比对靶子；实时卡在 build（早卡早省返工），合体后校验在 archive（抓合并后违规，自动判断多分支开/单分支关）；`git_ref` 让 agent 知道基线版本，可主动判断基线新旧 |
| `brief.md` | brainstorm 阶段（必然产出物） | specify（自动喂 `spec.md`）；审计 / 交接；优化模式：build 子代理上下文 | 目标 / 决策+理由 / 被拒 / 开放问题 | 决策脉络是 spec 场景素材；只要跑了 brainstorm 即落盘（必然产出），下一步 specify 自动读取；优化模式跳过 specify 时，brief.md 仍为 build 子代理提供决策背景 |
| `spec.md` | specify 阶段（OpenSpec 场景） | plan（场景指针）；archive（delta 源） | `#### WHEN/THEN` 场景 | plan 每任务引用场景作验收锚点 → 任务可测不跑偏；archive 合并 diff 需当前 spec；优化模式跳过此产物，由验收清单 + brief.md（若选跑 brainstorm）替代 |
| `plan.md` | plan 阶段（writing-plans + spec-kit tasks） | build（worktree / subagent / executing）；archive（标记完成） | 自洽任务（id / 场景指针 / 验收 / 依赖 / test-first）+ 能力声明 | 每任务自洽 → 直接喂 superpowers 执行，避免执行时回头问；能力声明驱动 build 启用集合 |
| 代码 | build 阶段（落 worktree） | archive（delta 合并源）；运行时 | 实现 diff | OpenSpec archive 合并代码变更 + spec diff；运行时是最终交付；优化模式不走 OpenSpec archive，改用 git diff |
| archive 记录 | archive 阶段（OpenSpec delta 或优化模式 git commit + stub） | 后续轮次（变更追溯 / 基线复用） | 合并后的演进史 | delta 累积成项目演进史，供未来特性复用上下文，闭环到下一轮；优化模式记录标记"未走 OpenSpec archive 原生命令"+ stub 入口保持统一 |
| 验收清单（优化模式，v6.7） | build 阶段启动时 Dispatcher 自动生成 | build 子代理（自测验收）；archive 清账（检查全勾选） | 3-5 条验收项 | 替代 spec.md 的 WHEN/THEN 作验收锚点（弱化但不为空）；未全勾选 archive 转人工 |
| OpenSpec stub / 改动范围说明（优化模式，v6.7） | archive 阶段（优化模式） | 后续轮次（追溯入口） | 模式标记 + git commit hash + 改动概览 | 防止"部分特性在 OpenSpec、部分只在 git log"的分裂；改动范围说明为后续迭代提供快速定位 |

**隐含消费边（不落文件但驱动行为）：**
- `plan.md` 上的能力标记（`test-first` / worktree 声明）→ build 的 Dispatcher 读取，决定本轮启用哪些能力（"按需 = 使用者声明"）。
- 优化模式下能力声明在确认环节采集，替代 plan.md 标记。
- `spec.md` 的 `WHEN/THEN` → build 子代理自测时的验收依据（隐性消费）；优化模式无此产物，由验收清单替代。
- 优化模式：`brief.md`（若选跑 brainstorm）→ build 子代理上下文，理解决策脉络（隐性消费，v6.7 明确）。
- `constitution.md` 指针 → build 子代理"真改变行为时"才读取，非每任务强读（惰性）；**优化模式策略一致**。
- 基线快照的 `git_ref` → build agent 判定时参考，基线明显过期则主动转人工。

### 5.2 自动读取契约（Auto-Read Contract）

上一节是"消费关系映射"，本节把消费关系**从可选项升级为强制行为**：**每个阶段开工前，必须自动读取它所需的上游产物，禁止在缺上下文的情况下动手。** 这是桥接层对 Dispatcher 的硬约束（非建议），目的是消除"agent 忘了加载上游、凭空开干"这一最常见的失误来源——也是第 2 点的直接落地。

| 即将启动的阶段 | 必须自动读取的上游产物 | 读取内容 / 用途 |
|---|---|---|
| **specify** | `brief.md`（若跑过 brainstorm） | 决策脉络 → 生成 `spec.md` 场景素材 |
| | `constitution.md` 指针 | 质量 / 测试 / UX / 性能原则约束 |
| **plan** | `spec.md` | 场景来源，每任务挂"场景指针" |
| | `brief.md`（若有） | 决策脉络，确保任务不偏离已决方向 |
| | `constitution.md` 指针 | 原则约束 |
| **build** | `plan.md` | 任务清单 + 能力声明（驱动启用集合） |
| | `spec.md` 的 `WHEN/THEN` | 子代理自测验收依据 |
| | 结构基线快照（含 `git_ref`） | 实时结构门禁比对靶子 + 基线版本判断 |
| | `constitution.md` 指针（惰性） | 真改变行为时才读 |
| **build（优化模式，v6.7）** | 需求描述 + 确认优化模式时的对话 | 验收清单生成输入源 |
| | 能力声明（确认时采集） | 替代 plan.md 能力声明，驱动 build 启用集合 |
| | `brief.md`（若选跑了 brainstorm，v6.7 明确消费） | 决策脉络 → build 子代理上下文 |
| | 验收清单（v6.7，Dispatcher 自动生成） | build 子代理自测验收（3-5 条） |
| | 结构基线快照（含 `git_ref`） | 实时结构门禁比对靶子 |
| | `constitution.md` 指针（惰性，与常规模式策略一致，v6.7 统一） | 真改变行为时才读 |
| **archive** | `plan.md`（优化模式豁免） | 标记任务完成 |
| | `spec.md`（优化模式豁免） | delta 合并源（优化模式无 spec delta，绕过 OpenSpec archive 原生命令，改用 git commit + OpenSpec stub） |
| | `brief.md`（若有，含优化模式选跑 brainstorm 的场景） | 归档作决策脉络追溯 |
| | `constitution.md` | 原则证据核查（优化模式仍做） |
| | 转人工挂起清单 | 必须全部清账（通过 / 调整）才允许归档 |
| | 验收清单（优化模式） | 检查全勾选，未全勾选 → 转人工 |
| | 结构基线快照（自动判断） | 合体后校验比对靶子（自动判断多分支才开，见 §3.2.4；优化模式默认关） |

**强制规则：**
- **缺必读产物不开工**：若应有上游产物缺失（如 specify 前 `constitution.md` 未生成、`brief.md` 标称应有却不在），Dispatcher 拒绝进入下一阶段，先补产物。这与价值闸门互补——价值闸门决定"要不要产"，自动读取契约保证"产了就一定被消费"。
- **只读所需**：auto-read 读取的是"指针 / 指定字段 / 指定文件"，不是把上游整篇塞进上下文（与 L2 惰性指针一致，避免 token 膨胀）。
- **读取即消费**：读到的内容直接驱动本阶段行为（场景 → 任务、能力声明 → 启用集合、基线 → 门禁），不读就做不出正确行为。

---

## 6. 桥接拓扑（Facade + Adapter + Dispatcher）

```
        ┌─────────────────────────────────────────┐
        │            SpecPowers Facade            │  门面：定序 + 触发
        ├─────────────────────────────────────────┤
        │  Dispatcher（按阶段路由 + 惰性指针注入） │
        ├──────────┬──────────┬───────────────────┤
        │ Adapter  │ Adapter  │ Adapter           │  适配器：格式翻译
        │ OpenSpec │ spec-kit │ superpowers       │
        └──────────┴──────────┴───────────────────┘
              │           │            │
        ┌─────┴────┐ ┌────┴─────┐ ┌───┴──────────┐
        │ OpenSpec │ │ spec-kit │ │ superpowers  │  原生引擎（不改动）
        │ 引擎     │ │ 引擎     │ │ 技能集       │
        └──────────┘ └──────────┘ └──────────────┘

  桥接层独有：结构基线扫描（带 git_ref）+ 实时门禁（三态）+ archive 合体后校验（自动判断）+ 优化模式（agent 判小 + `--fast` / 使用者确认 + 内建回退 + 最小验收清单）
```

---

## 7. 在原有流程里的归属

插件不塞进任何一套框架，而是它们的**编排者（门面）**——对 spec-kit 借用 constitution + tasks 生成；对 OpenSpec 借用 specify 场景格式 + archive（优化模式绕过）；对 superpowers 借用执行纪律技能。插件自身属于流程的串联与强制层，不替代任何核心引擎。

---

## 8. 锁定的决策

### 8.1 核心决策（架构层，稳定不变）

1. constitution 由 `/speckit.constitution` 生成，聚焦质量/测试/UX/性能四类（v5 的"结构基线塞进 constitution"撤回）。
2. 结构一致性门禁独立为桥接层硬规则，基线由插件扫描生成（constitution 阶段**一次扫死**，不增量刷新，接受误杀；基线带 `git_ref` 给 agent 额外信号）。
3. 实时门禁三态：通过 / 转人工 / 打回；**维持 agent 主观判断**，不引入客观化清单（接受跨 agent 不可重复、无法回归测试的代价，换取零配置开箱即用）。
4. 设计先定，实现后谈——本文件不含任何实现代码。

### 8.2 流程决策（阶段产物层）

5. **build 能力由使用者决定**：四技能是能力池，"按需"的主语是**使用者**——框架不按规模自适应开/关，只执行使用者的显式声明（plan.md 能力标记 / 优化模式确认时采集）；`executing-plans` 默认 conductor，使用者可在优化模式确认时一并决定是否跳过编排。
6. **头脑风暴提升为编号阶段 1（可选，高权重）**：位置在 constitution 之后、specify 之前；触发权在用户。产物——原始探索不持久化（违反价值闸门），仅提炼为 `brief.md`（目标 / 决策+理由 / 被拒方案 / 已决开放问题），**且为必然产出物**：只要跑了 brainstorming 就落盘。**提炼采用方案 1 结构化收口**（见 §2.1.1.1）。
7. **自动读取契约**：上一阶段产出物在下一阶段**必须自动读取所需内容**（见 §5.2），缺必读产物 Dispatcher 不开工；价值闸门决定"产不产"，自动读取契约保证"产了必被消费"。
8. **archive 桥接职责澄清**：复用 OpenSpec delta 合并；桥接额外做**四件**——清账转人工挂起、constitution 原则证据核查、收尾产物、**合体后结构校验**（v6.6 改为 archive 阶段自动判断，多分支开单分支关）；archive 阶段**只转人工不打回**；不重跑全量结构门禁、不重写 spec。**优化模式 archive 绕过 OpenSpec 原生命令**（无 spec.md），改用 git commit + OpenSpec stub + 改动范围说明，仍做原则核查，跳过合体后校验。

### 8.3 v6.5/v6.6/v6.7 增量决策

9. **（v6.5）结构门禁零配置化**：判据不外置为规则文件，判据源不依赖任何框架内部模型（git + 通用文件），开箱即用、零学习门槛。
10. **（v6.6→v6.7）优化模式**：新增"真正跳过 specify + plan"的快速通道，覆盖 bugfix / 极小改动场景。触发机制为 agent 判断是否为小特性——判小则提示使用者确认后走优化模式（含能力声明采集 + brainstorm 可选），其他默认走完整流程（v6.6）；v6.7 新增 `--fast` 显式入口、结构化理由、内建回退、最小验收清单。archive 退化为 git commit + OpenSpec stub + 改动范围说明（无 spec delta）；判据聚焦需求性质不判改动行数。
11. **（v6.6）archive 合体后校验改为自动判断**：从"默认强制 / 使用者声明开关"改为"archive 阶段自动检测"——多分支 delta 合并自动开，单分支自动关；使用者零负担，无需事前声明；优化模式默认关（不走多分支合并）。build 实时门禁抓单点违规（早卡），archive 校验抓合并后违规（增量），二者互补非冗余。
12. **（v6.6）削减概念暴露面**：对外只暴露 3 个核心概念（流水线 / 结构门禁 / 能力声明）+ 2 个开关；Facade/Adapter/Dispatcher/git_ref/惰性指针/价值闸门/自动读取契约作为设计内幕（见 §0.1）。
13. **（v6.6）砍掉直跑模式**：v6.5 的全阶段直跑模式被优化模式取代——优化模式已覆盖小特性场景，直跑作为"中间态"存在价值弱；砍掉后小特性走优化模式、其他走完整流程，二选一无中间态，规则最简。

14. **（v6.7）优化模式下 brief.md 消费者明确**：优化模式跳过 specify，若选跑 brainstorm 产 brief.md，消费者切换为① build 子代理上下文（理解决策脉络，减少回头问使用者）+ ② archive 审计/交接。仍满足价值闸门（见 §2.1.1）。
15. **（v6.7）优化模式 archive 入口统一**：优化模式 archive 不走 OpenSpec 原生命令，但在 OpenSpec archive 目录下落一条 stub 记录（git commit hash + 改动范围说明），保持所有特性的 archive 入口统一可查，避免两套体系分裂（见 §2.2）。
16. **（v6.7）优化模式内建回退路径**：build 中途可声明"升级为完整流程"，回退到 specify 阶段，已写代码保留作参考。只允许一次切回，不可往返（见 §4.2.1）。
17. **（v6.7）优化模式最小验收清单**：build 阶段 Dispatcher 自动生成 3-5 条验收项，替代 spec.md 作验收锚点。不接受 <3 条（太少无意义），>5 条触发回退提示（可能不是小特性）。archive 时检查全勾选（见 §4.2.2）。
18. **（v6.7）优化模式显式入口 `--fast`**：使用者可直接声明走优化模式确认，不依赖 agent 判断（见 §4.2）。
19. **（v6.7）优化模式确认交互结构化理由**：agent 判小特性的理由必须按格式 `[理由：<信号>/...，置信度：<高/中/低>]` 输出，信号集合固定 {bugfix, 单文件, 无新场景, 纯配置, 纯文案, 纯重构}（见 §4.2）。
20. **（v6.7）基线刷新阈值相对化**：从固定 50 commit 改为——① 基线 > 30 天 ② 基线距 HEAD commit 数 > 项目月均 2 倍。任一触发即提示（见 §3.2.1）。
21. **（v6.7）brief.md 最低信息密度恢复**：从"至少目标段"恢复为"至少目标 + 至少一段其他"，保证最低信息密度（见 §2.1.1.1）。
22. **（v6.7）事后抽样审计加下限**：10% 抽样率 + 绝对下限"每 20 次 build 至少抽审 1 次"，取 max（见 §3.2.3）。
23. **（v6.7）constitution 注入策略统一**：常规模式和优化模式均采用惰性指针注入，消除两套模式下的隐式行为差异（见 §4.2）。

### 8.4 v6.8 增量决策（交付模型调整，实现方案回写）

24. **（v6.8）交付模型锁定为 skill 插件**：最终交付物为全新 WorkBuddy **skill 插件**，用户以 `/specpowers.*` slash 命令调用，不再是对外 `specpowers` 二进制 CLI；内部确定性工作由 skill 经 Bash 调 `bridge/` Python helper 完成，对用户透明（见 §0.1、§4.2）。
25. **（v6.8）调用命名空间统一 `specpowers.`**：对齐 `/speckit.constitution` 框架级命名惯例，避免与 superpowers 裸名技能（brainstorming 等）冲突（见 §0.1）。
26. **（v6.8）阶段即显式命令**：各阶段为独立 slash 命令（`/specpowers.brainstorm` / `/specpowers.specify` / `/specpowers.plan` / `/specpowers.build`），命令名即阶段名，无"无参自动推进"魔法；跳过探索时用户直接调 `/specpowers.specify`，不再有"是否跑 brainstorm"的交互提问（见 §4.2）。
27. **（v6.8）回退降级为运行时自然语言交互**：build 中途声明「升级为完整流程」不再暴露为 `specpowers upgrade` 命令，仅作运行时交互（方案 A）（见 §4.2.1）。
28. **（v6.8）新增调试命令 `/specpowers.status`**：打印当前 stage / mode / fallback_used，不计入概念暴露面。
29. **（v6.8）仅保留两个带 flag 命令**：constitution `--force`（入口护栏/重建）与 archive `--force-merge-check`（出口闸门/强制合体后校验），其余命令无 flag（见 §2.2、§4.2）。
30. **（v6.8）`bin/specpowers` 薄壳化**：对外不可见，仅供 skill 经 Bash 调度 `bridge/` 确定性模块；用户面统一为 slash 命令（见 §4.2）。

---

## 9. 实现阶段待办（设计确认后启动，本回合不做）

- [ ] 把 v2 dispatcher 降级为 Facade + 三个 Adapter
- [ ] 接 constitution 第一步（调 `/speckit.constitution`）
- [ ] 实现结构基线扫描（带 `git_ref`）+ 实时门禁（三态，agent 主观判断）
- [ ] **实现基线刷新触发**（constitution.md 修改自动重扫 + 手动 `/specpowers.baseline` 命令 + git_ref 相对阈值过期提示：30 天或月均 commit 数 × 2）
- [ ] 实现 archive 合体后结构校验（复用基线 + 三态，只转人工不打回，**archive 阶段自动判断多分支开/单分支关**；检测方式：git log 有 merge commit 则开）
- [ ] `writing-plans` 加瘦身约束（合并 spec-kit tasks 进 plan.md）
- [ ] 五位 superpowers 技能接线（worktree / subagent / executing / TDD）
- [ ] **实现优化模式**（agent 判断是否为小特性 → 判小则给使用者提示确认（含结构化理由 `[理由：<信号>/...，置信度：<高/中/低>]`）+ 采集能力声明（仅 TDD，禁止 worktree/subagent）+ 可选 brainstorm → 确认后跳过 specify/plan；前置检查 constitution.md + 基线快照存在；archive 退化为 git commit + OpenSpec stub + 改动范围说明，不做 spec delta；archive 记录标记模式 + 使用者确认）
- [ ] **实现优化模式内建回退**（build 中途可声明"升级为完整流程" → 暂停 build → 保留代码 → 回到 specify → 按完整流程走；只允许一次切回）
- [ ] **实现 `/specpowers.fast` 显式入口**（使用者直接声明走优化模式确认，跳过 agent 判断）
- [ ] **实现最小验收清单生成**（build 启动时 Dispatcher 基于需求描述 + 对话生成 3-5 条 checklist；少于 3 条拒绝、多于 5 条提示回退；archive 检查全勾选）
- [ ] **实现 archive OpenSpec stub 入口**（优化模式 archive 在 OpenSpec 目录落 stub 记录：模式标记 + git commit hash + 改动范围说明）
- [ ] **实现改动范围说明生成**（3-5 句，概述改了什么文件、影响了什么行为）
- [ ] **准备优化模式判断测试用例集**（10-20 个需求描述 + 期望判定：小/非小/边界），改 agent prompt 后回归验证判断准确率
- [ ] 最小骨架跑一个真实特性验证体感 + token 实测
