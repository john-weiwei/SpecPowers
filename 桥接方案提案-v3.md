# SpecPowers v3 — 桥接层方案提案

> 状态：提案待评审（老板提出三点修正，本提案据此重构）
> 一句话定位：**不造第 4 套框架**。SpecPowers 退回到薄桥接层——定义一份最小共享契约，把 OpenSpec / spec-kit / superpowers 各自的能力按需适配、路由、编排起来。

---

## 为什么从 v2 再退一步

v2 把自己当成了"新框架"——重写 dispatcher、内置 4 个 skill、自管 constitution。这违反一条架构铁律：**能适配就别重写**。

三套框架的核心能力已经很成熟：
- OpenSpec = spec 格式 + change/archive 机制
- spec-kit = workflow 编排 + task 生成
- superpowers = skill 组合 + 开发纪律

重写既浪费 token（你在第 3 点确认省 token 已成功，就更不该重造），又制造维护债。桥接层只做它们做不好的事：**跨框架翻译 + 路由 + 极简契约**。

---

## 架构：Facade + Adapter + Dispatcher

```
         ┌──────────────────────────────┐
         │   共享契约 (spec.md+tasks.md)  │  ← 唯一急切传递的产物
         └──────────────┬───────────────┘
                        │
         ┌──────────────▼───────────────┐
         │   桥接层 SpecPowers           │
         │  Facade(门面) + Dispatcher    │
         │  ┌────────┬────────┬────────┐ │
         │  │OpenSpec│spec-kit│superpw.│ │  ← Adapter 适配器
         │  │Adapter │ Adapter│ Adapter│ │
         │  └────────┴────────┴────────┘ │
         └──────┬──────────┬────────┬────┘
                │          │        │
        ┌───────▼┐  ┌──────▼──┐ ┌────▼─────┐
        │OpenSpec│  │ spec-kit│ │superpowers│  ← 原生框架，能力不动
        └────────┘  └─────────┘ └──────────┘
```

- **Facade（门面）**：对外只暴露 `specify / build / archive` 三个动作，内部决定调谁。
- **Adapter（适配器）**：每个框架一个，把它的原生格式 ↔ 共享契约互转。
- **Dispatcher（调度器）**：按任务类型路由到对应适配器，**按需加载**，不预载全部。

---

## 回应你的三点

### ① TDD 不强制、头脑风暴不强制
把 v2 的"原则强制 / 流程可选"再退一格 → **原则记录、不强制**。

- 砍掉"无测试不合并"硬门禁，改为 `tasks.md` 里一行记录：
  `测试：跳过（理由：纯前端样式调整，无逻辑分支）`
- 头脑风暴默认关，需要时才开。
- 桥接层的职责是**把决定记下来（含跳过理由）**，不是卡住流程。审计可追溯，但不拖节奏。

### ② Context 注入：标签不是最优解
v2 用 `<context>` 把原则整段塞进每阶段——这是"急切全量注入"，token 贵且易漂移。

**改用"惰性指针（lazy pointer）"取代标签：**
- 不维护标签分类法。标签本身要持续同步、要花 token 去保持一致性，是隐性成本。
- 只传一份极小的 **context-manifest**（约 20 行），给的是**指针**不是内容：

```yaml
# context-manifest.yaml（极简，随任务生成）
spec: ./spec.md#scenarios
tests: ./tests/            # 仅指针，不内联
constitution: ./constitution.md   # 子代理按需读
```

- 子代理只在"真的会改变它行为"时才去读对应文件。上下文有没有意义，由子代理按指针**按需拉取**判断，而不是主控提前猜。
- 结论：对线性 spec-driven 流程，**显式指针优于隐式标签**。标签只在你需要检索 / 跨任务复用时才划算——而桥接层当前不需要。

### ③ 桥接而非新建
- SpecPowers 不实现 spec 解析、不实现 task 生成、不重新发明 skill——全部调原生框架。
- 省 token 的来源变成三层叠加：
  1. 不重写逻辑（复用三套成熟引擎）
  2. 惰性指针（context 不内联）
  3. 2 产物契约（spec.md + tasks.md）
- "只做桥接"的边界：
  - **做**：共享契约定义、适配器、路由、archive 时的跨框架 diff 合并
  - **不做**：替换任何一套框架的核心引擎

---

## 与 v2 的差异清单

| 项 | v2 | v3（桥接） |
|---|---|---|
| 定位 | 新框架 | 薄桥接层（Facade+Adapter） |
| 原则 | 强制（含 TDD 门禁） | 记录、不强制 |
| 头脑风暴 | 默认关 / 可选 | 可选（同） |
| Context | `<context>` 急切全量注入 | 惰性指针 manifest |
| 核心能力 | 重写 dispatcher + 4 skill | 适配器调原生框架 |
| 维护债 | 高 | 低 |

---

## 待你拍板

1. **跨框架 diff 合并**：archive 时是否要把三套的产物差异合并记录？还是桥接层只做路由、不做合并？
2. **指针颗粒度**：context-manifest 按文件指针够不够，还是细到 section 级（如 `#scenarios`）？
3. **路由优先级**：默认顺序 OpenSpec → spec-kit → superpowers，还是按任务类型动态选适配器？

> 若三点都认可，下一步把 v2 的 dispatcher / skill 文件**降级为适配器**，"原则层"降为按需指针，删掉 `<context>` 急切注入即可落地。
