# SpecPowers 桥接插件 · 实现方案

> 配套 `桥接插件设计方案-v6.7.md`（做什么/为什么）与 `技术架构方案-桥接插件.md`（骨架/契约）。
> 本文档定 **"模块怎么拆、文件怎么放、接口长什么样、按什么顺序写、怎么验"**，是设计确认后落地的施工蓝图。
> 范围：仅确定性执行层（Python 内部 helper，仅 stdlib，被 skill 调用）+ 认知层（作为 AI 编码 agent 的 **skill 插件**加载）。**不重写任何框架引擎**，不引入第三方依赖。

---

## 0. 分层回顾与边界

| 层 | 技术 | 职责 | 本方案交付物 |
|---|---|---|---|
| 认知层 | Markdown 指令集（作为 AI 编码 agent 的 **skill 插件**加载，用户以 `/specpowers.*` 调用） | 定序、触发、agent 主观判定（门禁三态 / 判小 / brief 提炼）、自动读取契约强制、确认交互 | `SKILL.md` + `prompts/` 7 个契约文件 |
| 确定性层 | Python 3.11+（仅 stdlib），**内部 helper（非对外 CLI）** | 基线扫描、git_ref、刷新、stub 生成、抽样记账、状态机、文件锁、原子写；由 skill 经 Bash 调用 | `bridge/` 全部模块（内部）；`bin/specpowers` 仅作 skill 内部薄壳 |

**一律不碰**：DB、配置文件、规则文件、框架内部模型。产物即 markdown，git 跟踪即可追溯。

**安全底线**：所有用户输入（需求文本、对话内容、文件路径）一律参数化传递，禁止拼入 shell 命令字符串。`git_util.py` 用参数列表调用 subprocess，其余模块同理。

**团队协作前提**：本插件设计为团队共享工具。基线产物 `baseline.json` 提交到仓库供全员共享，个人流水线状态 `state.json` / `audit.log` 本地保留。详见 §2。

---

## 1. 目录结构与文件布局

```
specpowers/                       # 插件源码根（作为一个独立 skill 包分发）
├── SKILL.md                      # 门面 + Dispatcher 触发契约（认知层总入口）
├── bin/
│   ├── specpowers                # 内部薄壳 Shell 脚本（Linux/macOS）
│   └── specpowers.bat            # 内部薄壳批处理（Windows）
├── bridge/                       # 确定性执行层（Python，仅 stdlib）
│   ├── __init__.py
│   ├── facade.py                 # Facade：参数解析 + 定序
│   ├── dispatcher.py             # Dispatcher：阶段路由 + 自动读取契约 + 能力调度
│   ├── adapters/
│   │   ├── __init__.py
│   │   ├── openspec.py           # to_scenario / archive_delta / write_stub
│   │   ├── speckit.py            # gen_constitution / gen_tasks（触发 shell + 解析产物）
│   │   └── superpowers.py        # run_skill / persist_brief
│   ├── modules/
│   │   ├── baseline_scanner.py   # scan() / refresh 三路
│   │   ├── structure_gate.py     # extract_signals() / record_decision()
│   │   ├── mode_controller.py     # build_confirm_prompt / parse_confirm / gen_checklist / fallback
│   │   ├── archive_auditor.py     # audit / write_stub / change_summary / merge_check_detect
│   │   ├── sampling_auditor.py    # log / sample / should_force
│   │   └── artifact_registry.py   # MANIFEST 常量 / required_for / has_consumer
│   └── core/
│       ├── errors.py             # 统一异常定义（FatalError / RecoverableError）
│       ├── git_util.py           # subprocess 安全封装（参数列表 + 超时分级 + 前置校验）
│       ├── lock.py               # 文件锁（.lock + rename 原子）
│       ├── fs_state.py           # state.json 原子读写
│       └── platform.py           # 平台检测与适配（Linux/macOS/Windows 差异收口）
├── prompts/                      # 认知层契约（被 host agent 加载）
│   ├── constitution.md           # 触发 /speckit.constitution + 基线扫描调度
│   ├── brainstorm.md             # 含收口契约（结构化 summary → brief.md 落盘）
│   ├── specify.md                # OpenSpec 场景 + 自动读 brief/constitution 指针
│   ├── plan.md                   # writing-plans 瘦身约束（合并 spec-kit tasks）
│   ├── build.md                  # 实时门禁三态契约 + 能力池调度 + 验收清单消费
│   ├── archive.md                # 四职责 + stub + 改动范围说明
│   └── fast_mode.md              # 优化模式：判小 prompt / 确认交互 / 回退 / 清单
└── tests/
    ├── judge_tests.json          # 判小测试集（格式见 §9.1）
    ├── test_git_util.py
    ├── test_baseline_scanner.py
    ├── test_merge_check.py
    ├── test_checklist.py
    ├── test_fs_state.py
    ├── test_lock.py              # 死锁检测 / 锁争用测试
    └── test_platform.py          # 平台检测适配测试

.specpowers/                      # 运行时内部存储
  ├── baseline.json               # 【团队共享】提交到 git，全队统一基线
  ├── state.json                  # 【本地】gitignore，每人独立流水线状态
  ├── audit.log                   # 【本地】gitignore，每人独立审计日志
  └── .lock                       # 【本地】gitignore，防重入锁
```

> **Git 策略**：`baseline.json` 提交到仓库供团队共享；`state.json` / `audit.log` / `.lock` 加入 `.gitignore`，每人本地独立。这保证团队基线一致，同时各自流水线状态互不干扰。详见 §2。

---

## 2. 团队协作策略（核心）

> SpecPowers 是**团队工具**，多人协作场景是设计一等公民。以下策略确保团队成员在同一基线上工作，状态互不干扰。

### 2.1 产物归属

| 产物 | 归属 | Git 策略 | 原因 |
|---|---|---|---|
| `baseline.json` | **团队共享** | 提交到仓库 | 结构基线（top_dirs/deps/src_patterns）是项目级事实，所有人应比对同一靶子 |
| `constitution.md` | **团队共享** | 提交到仓库 | 项目原则全员一致 |
| `state.json` | **个人本地** | `.gitignore` | 每人流水线进度独立（你卡在 build，同事已 archive） |
| `audit.log` | **个人本地** | `.gitignore` | 审计日志本机累积，不污染仓库历史 |
| `.lock` | **个人本地** | `.gitignore` | 锁文件仅本机有效 |

### 2.2 baseline.json 共享机制

- **首次加入项目**：`git clone` 后 `baseline.json` 随仓库下发。`/specpowers.constitution` 无参调用时检测到已存在 → 跳过扫描（不重复生成）。
- **基线更新**：任何人改 `constitution.md` 后，下一个人跑任意**阶段入口命令**（`brainstorm`/`specify`/`fast`）时，**阶段开始前** Dispatcher 检测 `constitution_changed()` → 自动触发 lazy 重扫 → 生成新的 `baseline.json`。重扫完成后输出提示"baseline.json 已更新，请 review 后提交"——**由用户手动 git commit**，agent 不自动提交（避免静默写入仓库历史）。注：`plan`/`build` 等中段命令不触发 lazy 重扫，避免打断已进行中的流水线。
- **手动刷新**：`/specpowers.baseline` 显式重扫 → 提交。

### 2.3 基线漂移检测

```
你的 baseline.json.git_ref vs HEAD：
  ├─ 差距 ≤ 项目月均×2 或 30天 → 视为新鲜，无提示
  └─ 差距 > 阈值 → Dispatcher 开工提示 "基线可能过时，建议 /specpowers.baseline"
```

- 阈值计算：`max(30天, 项目月均commit数×2)`，无历史数据兜底 30 天。
- 漂移提示**不阻断**流水线，仅 warning。

### 2.4 多人并发策略

- **不同 feature 并发**：无冲突。每人独立 `state.json`，各自在不同 feature 上推进。
- **同一 feature 多人协作**（如 pair programming）：`state.json` 不共享，各自独立跑流水线到 build。任何人完成编码后均可调 archive。Dispatcher 在 archive 前检查该 feature 是否已有 archive commit（`git log --grep="specpowers.*${feature}"`），已存在则**拒绝重复归档**并提示 `git pull && /specpowers.reset` 同步。谁是「最后一人」由实际工作节奏自然决定，不是人为约定。
- **baseline.json 并发修改**：两人同时改 constitution 触发重扫 → git merge 时按标准冲突解决（JSON 文件，冲突概率低；若冲突，取扫描时间更新的版本）。
- **跨机器**：同一开发者在不同机器 clone 同一仓库 → state.json 不跟随，每台机器独立初始化。这是设计选择（不引入中心化状态服务）。

### 2.5 .gitignore 模板

```gitignore
# SpecPowers 个人运行时（不提交）
.specpowers/state.json
.specpowers/audit.log
.specpowers/.lock

# baseline.json 是团队共享的 —— 不在此忽略，正常提交
```

---

## 3. 安装与分发

> SpecPowers 是一个**通用 AI 编码 agent 插件**，不绑定任何特定平台。WorkBuddy、Claude Code、Cursor、GitHub Copilot 等支持 skill/rule/command 机制的 agent 均可加载。

### 3.1 插件包结构

分发包即 `specpowers/` 目录本身。用户获取该目录后放入 agent 的技能/规则加载路径即可。

```
specpowers/          ← 分发包根目录
├── SKILL.md         ← agent 加载入口
├── bin/
│   ├── specpowers     ← Shell 脚本（Linux/macOS）
│   └── specpowers.bat ← 批处理（Windows）
├── bridge/          ← Python 确定性执行层
├── prompts/         ← 认知层契约
└── tests/           ← 测试（可选安装）
```

- **依赖**：Python 3.11+（仅 stdlib，无需 pip install）；git 2.30+。
- **体积**：纯文本 + Python，预计 < 200KB。

### 3.2 支持的宿主平台

| 宿主 Agent | 加载方式 | 配置路径 |
|---|---|---|
| **WorkBuddy** | 放入用户/项目技能目录 | `~/.workbuddy/skills/specpowers/` 或 `{project}/.workbuddy/skills/specpowers/` |
| **Claude Code** | 自定义 slash command | `.claude/commands/specpowers/` |
| **Cursor** | 项目规则（.cursorrules）引用 | `.cursor/rules/specpowers/` |
| **GitHub Copilot** | 自定义指令集成 | `.github/copilot-instructions.md` 中引用 |
| **其他 agent** | 任何支持 Markdown 指令集加载的平台 | 将 `SKILL.md` 注册为该平台的 skill/command/rule |

> 核心原则：SpecPowers 不包含任何平台特定的 API 调用或 SDK。认知层是纯 Markdown 指令集，确定性层是纯 Python stdlib。适配新平台只需将 `SKILL.md` 注册为该平台的指令源。

### 3.3 安装步骤

```bash
# 方式一：Git 克隆（推荐）
git clone <specpowers-repo-url> ~/.ai-agent/skills/specpowers/

# 方式二：手动下载
# 解压 specpowers.zip 到 agent 的技能目录

# 方式三：WorkBuddy 技能市场（如已上架）
# /install specpowers
```

安装后验证：

```bash
cd /path/to/your/project
python ~/.ai-agent/skills/specpowers/bridge/facade.py --help   # 确定性层可用
git --version                                            # ≥ 2.30
```

### 3.4 版本策略

- 语义化版本：`vMAJOR.MINOR.PATCH`
- `SKILL.md` 顶部声明版本号，agent 加载时可校验
- 确定性层 `bridge/` 内部也有 `__version__`
- 认知层契约格式变更 → MAJOR bump；新增阶段命令 → MINOR bump；bugfix → PATCH bump
- **升级**：`git pull` 或重新下载覆盖（`.specpowers/` 运行时数据不受影响）

### 3.5 依赖兼容矩阵

| SpecPowers 版本 | Python | Git | spec-kit 兼容版本 | OpenSpec 兼容版本 |
|---|---|---|---|---|
| v1.x | ≥ 3.11 | ≥ 2.30 | spec-kit v2.x | OpenSpec v1.x |
| （后续版本按实际兼容性更新此表） |

> spec-kit / OpenSpec / superpowers 作为**被调用方**，SpecPowers 只消费其稳定输出（markdown 文件 / CLI stdout），不依赖内部 API。因此兼容窗口较宽。

---

## 4. 确定性执行层接口契约（写什么）

### 4.0 调用链总览

```
用户输入 /specpowers.build
       │
       ▼
┌──────────────────────────────────────┐
│  Agent（WorkBuddy / Claude / ...）    │
│  加载 SKILL.md → 识别为 skill 插件    │
│  加载 prompts/build.md 契约           │
│  根据契约指令执行                      │
│    │                                   │
│    ├─ 认知判定（门禁三态/判小/brief）   │  ← agent 自己做的
│    │                                   │
│    └─ 确定性操作（基线扫描/状态机/锁）  │
│         │                              │
│         ▼                              │
│   agent 调 Bash:                       │
│   $ python bridge/facade.py scan       │  ← 对用户透明
│   （或: bin/specpowers scan）           │     facade.py 是唯一入口
└──────────────────────────────────────┘
```

> **关键澄清**：
> - 确定性层的**唯一入口**是 `python bridge/facade.py <子命令>`。`bin/specpowers` 是可选薄壳（Shell 脚本），等价于 `python bridge/facade.py "$@"`，仅为减少敲字。
> - agent 实际执行的是 `python bridge/facade.py <子命令>`（或经薄壳），不是直接调 bridge 子模块。子模块由 facade 内部 dispatch。
> - 用户永远不直接敲这些命令，而是通过 `/specpowers.build` slash command 让 agent 去调。非 WorkBuddy 平台（如 Claude Code）对应的是该平台的 slash command 机制。

### 4.1 `core/git_util.py` — 安全封装（硬底线）
```python
def run_git(args: list[str], timeout: int = 30) -> str:
    """参数列表调用，禁止 shell=True / 字符串拼接；capture_output=True, text=True。
       超时分级：ls-tree/rev-parse 等轻量=30s；log --since 等范围=120s。
       所有调用方必须使用此封装，不得直接 subprocess.run。"""

def is_git_repo(root: Path) -> bool: ...
def is_detached_head(root: Path) -> bool: ...
def has_commits(root: Path) -> bool: ...
def git_exists() -> bool: ...                      # 缺失即报错退出，不让 agent 静默继续
def git_ref_of(root: Path) -> str: ...             # HEAD commit short hash
def diff_stat(root: Path, base: str | None) -> str:# git diff --stat（改动范围说明输入）
```

### 4.2 `modules/baseline_scanner.py`
```python
def scan(root: Path) -> dict:                      # 写 baseline.json，返回 dict
    # ① git ls-tree HEAD → top_dirs
    # ② 识别依赖清单：package.json/pom.xml/requirements.txt/go.mod/Cargo.toml… → deps
    # ③ 遍历 src/ 二级目录 → src_patterns（命名规律）
    # ④ 记录 git_ref + constitution_hash(sha256) + scanned_at

def constitution_changed(root: Path) -> bool:      # 重算 sha256 比对 baseline.constitution_hash
def refresh_baseline(root: Path) -> dict:          # 手动 refresh-baseline：重跑 scan()
def git_ref_stale(root: Path, baseline: dict) -> bool:
    # 相对阈值：距今>30天 或 距 HEAD commit 数 > 项目月均×2（无历史数据兜底 30 天）
def monthly_avg_commits(root: Path) -> int: ...    # 估算月均 commit（git log 近 90 天 /3）
```

### 4.3 `modules/structure_gate.py`
```python
def extract_signals(tree_diff: str, baseline: dict) -> list[str]:
    # 三类信号：新建基线没有的顶层目录 / 引入未知依赖 / 模式外新建架构层
    # 纯确定性提取，供 agent 三态判定参考（判定本身由 agent 在 build.md 契约内做）

def record_decision(root: Path, result: str, signals: list[str],
                    confidence: str, git_ref: str) -> int:
    # 追加写 audit.log 一条 JSONL，返回 seq；同时触发抽样判定（见 4.6）
```

### 4.4 `modules/mode_controller.py`
```python
def build_confirm_prompt(req: str, small: bool, signals: list[str], confidence: str) -> str:
    # 输出结构化确认文案（含 [理由：<信号>/...，置信度：<高/中/低>]）

def parse_confirm(text: str) -> dict:
    # 解析使用者确认结果：{go_fast, run_brainstorm, tdd}

def gen_checklist(req: str, dialog: str) -> list[str]:
    # Dispatcher 基于需求+对话生成 3-5 条验收项（实际文本由 agent 在 fast_mode.md 内产出，
    # 此处仅做边界校验：<3 拒绝、>5 提示回退）

def validate_checklist(items: list[str]) -> tuple[bool, str]:
    # 返回 (ok, hint)；>5 → "建议升级为完整流程"

def mark_fallback(state_path: Path) -> None:
    # fallback_used=true + 记录 fallback_count += 1
    # 整个 state 生命周期最多 fallback 1 次；reset 不清除 fallback 计数
```

### 4.5 `modules/archive_auditor.py`
```python
def is_feature_archived(root: Path, feature: str) -> bool:
    # git log --grep="specpowers.*${feature}" 检查该 feature 是否已有 archive commit
    # feature 参数已由 facade.py 经 normalize_feature() 规范化（见 §4.9），可直接用于 grep
    # 防御性：若调用方传入未规范化文本，此处不做二次规范化，上游保障
    # 防止同一 feature 被多人重复归档

def merge_check_detect(root: Path, last_archive_ref: str) -> bool:
    # 线索1：last_archive_ref..HEAD 有 merge commit → 多分支
    # 线索2：author/committer 去重计数>1 → 多作者（补 rebase/squash/cherry-pick 漏判）
    # 任一命中 → True；force_merge_check 参数可绕过自动判断强制 True

def write_stub(root: Path, mode: str, commit_hash: str, change_summary: str) -> None:
    # 优化模式在 OpenSpec archive 目录落 stub：模式标记 + hash + 改动范围说明

def change_summary_input(root: Path, base: str | None) -> str:
    # 产出 git diff --stat 统计，作为 agent 生成 3-5 句自然语言说明的输入

def audit(root: Path, mode: str) -> dict:
    # 四职责合集：清账转人工挂起 / constitution 原则证据核查 / 收尾产物 / 合体后校验调度
    # 前置：调用 is_feature_archived，已归档则拒绝并提示 git pull && /specpowers.reset
```

### 4.6 `modules/sampling_auditor.py`
```python
def log(seq: int, ts: str, stage: str, result: str, signals: list,
        confidence: str, git_ref: str) -> bool:
    # 写 audit.log；返回该次 sampled 是否=True（见 sample 规则）

def sample(seq: int) -> bool:
    # max(10% 随机, 每 20 次至少 1 次)：seq 满 20 且区间 sampled=true 次数=0 → 强制 True

def should_force(seq: int) -> bool: ...            # 供 log 内部调用
```

### 4.7 `modules/artifact_registry.py`
```python
# Artifact = {"layer": str, "path": str, "producer": str, "consumers": [str], "required": bool | dict}
#   layer: "cognitive" | "deterministic"
#   path: 相对仓库根的产物路径（如 "brief.md", "spec.md", "plan.md", "constitution.md"）
#   producer: 产生产物的阶段名（brainstorm/specify/plan/constitution）
#   consumers: 消费者阶段列表
#   required: True 表示所有模式必产；{"full": True, "fast": False} 表示模式维度控制
MANIFEST: list[Artifact] = [ ... ]
def required_for(stage: str, mode: str) -> list[Artifact]: ...   # 自动读取契约校验源
def has_consumer(artifact: Artifact) -> bool: ...  # 价值闸门兜底（延迟消费如追溯/audit 计为通过）
```

### 4.8 `core/fs_state.py` + `core/lock.py`
```python
# fs_state：state.json "读-改-写单事务"——先写临时文件再 rename 原子替换，避免半写损坏
def load_state(root: Path) -> dict: ...
def save_state(root: Path, state: dict) -> None: ...   # 原子写

# lock：写 .specpowers/.lock + rename 原子；拿不到锁即拒绝启动并提示"已有实例运行中"
def acquire_lock(root: Path) -> bool: ...
def release_lock(root: Path) -> None: ...
```

### 4.9 `facade.py` + `dispatcher.py`
```python
# facade 是 skill 内部薄壳：把 /specpowers.* slash 命令映射为子命令，调度 bridge/ 确定性模块
#   对外技能调用见 §12（/specpowers.constitution | .brainstorm | .specify | .fast | .plan | .build | .archive | .baseline | .reset）
#   内部子命令形式：constitution [--force] / brainstorm "<需求>" / specify "<需求>" / fast "<需求>" / plan / build / archive [--force-merge-check] / baseline / reset
#   内部工具子命令（非用户面，供 CI / agent 直接调用）：scan / gate [--base <ref>]（--base 指定比对基线，如 origin/main，缺省为 HEAD~1）
def main(argv: list[str]) -> int: ...

def normalize_feature(raw: str) -> str:
    # 入口命令收到 <需求> 后先规范化：取前 40 字符，去特殊字符，空格换"-"
    # 边界：空输入 → "unnamed"；纯中文无空格 → 原样截断（中文不替换）
    # 写入 state.json.feature 的是 normalized slug，确保后续 safe_grep / commit message 安全

# dispatcher.route(stage, mode)：阶段路由 + enforce_auto_read + 能力调度
def route(stage: str, mode: str) -> None:
    # 1) acquire_lock
    # 2) enforce_auto_read(required_for(stage, mode)) → 缺必读产物拒绝开工
    # 3) 加载能力声明（常规读 plan.md 标记；优化读确认采集）
    # 4) 调对应 Adapter / Bridge Module

def enforce_auto_read(required: list[Artifact]) -> None: ...
```

### 4.10 `adapters/*` — 外部框架接线

```python
# === openspec.py ===
def to_scenario(req: str, brief: str | None) -> str:
    # 产出 OpenSpec 场景格式文本，供 agent 写入 spec.md
def archive_delta(root: Path) -> None:
    # 调 OpenSpec CLI: openspec archive --delta（subprocess，参数列表安全调用）
    # 失败处理见 §4.11
def write_stub(root: Path, mode: str, commit_hash: str, change_summary: str) -> None:
    # 不调 OpenSpec CLI，直接写 markdown stub 文件到 OpenSpec archive 目录

# === speckit.py ===
def gen_constitution(root: Path, force: bool = False) -> None:
    # 通过 agent 调 /speckit.constitution slash command
    # 原因：spec-kit 的 constitution 生成依赖 LLM 交互，无法纯脚本化
    # 本函数只做前置校验（目录存在 / git 仓库），实际生成由 agent 执行认知层指令
def gen_tasks(spec_path: str) -> str:
    # 通过 agent 调 /speckit.plan 或等效的 task 生成逻辑
    # 原因同上：task 分解依赖 LLM，Adapter 只做校验 + 触发意图记录
    # 返回 tasks 文本模板提示，由 agent 合并进 plan.md

# === superpowers.py ===
def run_skill(name: str, ctx: dict) -> None:
    # 通过 agent 调对应的 superpowers slash command（如 /brainstorming）
    # 本函数只做参数校验 + 状态记录，实际执行由 agent 完成
def persist_brief(summary: str, root: Path) -> None:
    # 写 brief.md 到仓库根目录，不做二次抽取
```

> **Adapter 设计原则**：Adapter 本身不包含框架逻辑，只做三件事——① 参数校验 ② 触发对应的外部命令/slash command ③ 错误捕获与统一上报（见 §4.11）。如果外部框架只提供 agent 指令（无 CLI），Adapter 记录触发意图，由 agent 在认知层执行。

### 4.11 外部工具错误处理策略

SpecPowers 作为编排者，被调用的外部工具（spec-kit、OpenSpec、superpowers、git）可能失败。统一策略：

| 失败场景 | 严重程度 | 行为 |
|---|---|---|
| git 命令失败（仓库损坏/权限不足） | **致命** | 立即终止，输出错误信息，state 不变，要求人工介入 |
| `/speckit.constitution` 失败 | **致命** | 终止，提示检查 spec-kit 安装和项目状态 |
| OpenSpec delta 合并冲突 | **可恢复** | 暂停 pipeline，提示用户手动解决冲突后重试 archive（state 保持 build） |
| superpowers skill 调用失败 | **可恢复** | 记录失败原因到 stderr，转为人工模式（brainstorm→提示用户手动梳理思路） |
| baseline 扫描超时 | **降级** | 输出已收集的部分结果 + warning，不阻断后续阶段 |
| `is_feature_archived` 的 git log 无结果 | **正常** | 视为该 feature 未被归档，正常继续 |

> 所有错误统一经 `bridge/core/errors.py` 定义异常类型（`FatalError` / `RecoverableError`），facade 顶层 catch 后格式化输出。致命错误退出码非零；可恢复错误退出码零但 stderr 含警告。

### 4.12 跨平台兼容性（Linux / macOS / Windows）

| 项目 | Linux | macOS | Windows |
|---|---|---|---|
| Shell 环境 | bash（标准） | bash / zsh（bash 始终可用） | Git Bash 或 WSL |
| `bin/specpowers` | Shell 脚本 `#!/bin/bash` | 同 Linux，无需特殊处理 | 提供 `bin/specpowers.bat`（`@python bridge\facade.py %*`）作为薄壳 |
| 确定性层调用 | `python bridge/facade.py scan` | `python3 bridge/facade.py scan`（部分系统 `python` 指向 Python 2；优先用 managed Python 路径） | `python bridge/facade.py scan`（managed Python 路径） |
| Git | 原生 git（通过 PATH） | 原生 git（内置 `/usr/bin/git` 或 Homebrew `/opt/homebrew/bin/git`；均通过 PATH） | Git Bash 或 WSL 内 git，`git_util.py` 通过 PATH 查找 |
| 文件锁 | `fcntl.flock`（Linux 内核级互斥） | `fcntl.flock`（macOS BSD 层支持） | 降级为文件存在性检查（`os.path.exists(.lock)` + pid 校验），不保证内核级互斥 |
| 路径 | 正斜杠 `/` | 正斜杠 `/` | `pathlib.Path` 自动处理反斜杠；git 命令中路径用正斜杠 |
| 原子写 | `os.rename`（同文件系统原子） | `os.rename`（APFS/HFS+ 同卷原子） | `os.replace`（Python 3.3+ 同卷原子） |
| Python 版本检测 | `sys.version_info` 运行时校验 ≥ 3.11 | 同左；注意 Apple Silicon 需确认 arm64 Python | 同左 |
| 可执行权限 | `chmod +x bin/specpowers` | macOS Gatekeeper 首次运行可能弹窗，`xattr -d com.apple.quarantine bin/specpowers` 消除（可选） | 不需要（.bat 文件无此概念） |

> **跨平台优先原则**：bridge/ 纯 Python stdlib + pathlib，天然跨平台。差异化仅存在于三处——① 文件锁实现（Linux/macOS 用 `fcntl`，Windows 用文件存在性检查）；② 薄壳脚本（Shell vs .bat）；③ Python 命令名（`python` vs `python3`）。统一在 `bridge/core/platform.py` 中做平台检测和适配，其余模块不感知平台差异。

---

## 5. 认知层契约（Markdown 指令集）职责

| 契约文件 | 强制包含的硬规则 |
|---|---|
| `SKILL.md` | 总入口：定序 constitution→brainstorm→specify→plan→build→archive；声明 Facade 是编排者不替代引擎；削减概念暴露面（对外只 3 概念+2 开关）；指向 `prompts/` |
| `prompts/constitution.md` | 触发 `/speckit.constitution`；聚焦 4 类原则（质量/测试/UX/性能）；**不承载结构规则**；扫基线调度（BaselineScanner.scan） |
| `prompts/brainstorm.md` | 收口契约：结束必输出结构化决策摘要（目标+至少一段其他），**必然落盘 brief.md**；原始探索不持久化；内建判小信号映射表（{bugfix,单文件,无新场景,纯配置,纯文案,纯重构} → 示意） |
| `prompts/specify.md` | 调 OpenSpec 场景格式；自动读 brief.md(若有)+constitution 指针；缺必读不开工 |
| `prompts/plan.md` | writing-plans 瘦身约束：合并 spec-kit tasks 进 plan.md 单产物；任务单元字段固定（描述/场景指针/验收/依赖/test-first/cap） |
| `prompts/build.md` | 实时门禁三态判定契约（三信号→通过/转人工/打回，消息须含信号+证据）；能力池调度（executing 默认 conductor，worktree/subagent/TDD 由声明驱动）；优化模式消费验收清单 |
| `prompts/archive.md` | 四职责；只转人工不打回；**前置检查**：该 feature 已有 archive commit → 拒绝重复归档，提示 `git pull && /specpowers.reset`；优化模式绕过 OpenSpec 原生命令→git commit+stub+改动范围；合体后校验自动判断（多分支开/单分支关） |
| `prompts/fast_mode.md` | 判小信号集 {bugfix,单文件,无新场景,纯配置,纯文案,纯重构} + 置信度结构化输出；确认交互含能力声明(仅 TDD，禁 worktree/subagent)+brainstorm?；内建回退；最小验收清单 3-5 |

---

## 6. 数据契约落地（精炼自架构 §4.3）

**`baseline.json`**（**团队共享，提交 git**）：`{git_ref, scanned_at, top_dirs[], deps[], src_patterns[], constitution_hash}`
**`state.json`**（**本地，gitignore**）：`{stage, mode, fallback_used, fallback_count, feature, last_archive_ref}`
- 状态机：`constitution→ready→(brainstorm|specify)→specify→plan→build→archive→ready`；优化直接 `ready→build`；`build→specify`（回退，仅一次）
- 生命周期：无参冷启→视为 constitution 阶段自动建 state；`reset-state` 删 state+.lock（不碰 baseline/audit）
- **archive→ready 清场**：archive 成功后 state 跃迁到 `ready` 时，`feature` 字段清空（`""`），`mode` 重置为 `full`，确保下一轮 feature 从干净状态开始
- **回退计数**：`fallback_count` 记录在 `state.json` 中，`reset` **不清除**该计数。整个 state 生命周期（从首次 constitution 到下次 constitution）最多 fallback 1 次。防止通过 reset 绕过回退限制。

**`audit.log`**（JSONL）：`{seq, ts, stage, gate, result, signals[], confidence, sampled, git_ref}`
- 追加写、不删、天然并发友好；`seq` 单调递增支撑"每 20 次至少 1 次"下限

**原子性**：`state.json`/`baseline.json` 一律"临时文件 + rename"；`.specpowers/.lock` 防重入。

### 6.1 错误恢复

- **正常退出**：release_lock + state 写入完整。
- **用户中断（Ctrl+C）**：Python 注册 `SIGINT` handler → 保存当前 state（原子写）+ release_lock → 退出。确保中断时 state 不丢、锁不残留。
- **进程崩溃**（非信号终止）：`.lock` 残留，下次启动时 `acquire_lock` 检测到旧锁 → 检查持有锁的进程是否存活 → 已死则清理旧锁自动恢复；仍存活则提示"已有实例运行中"拒绝启动。
- **state.json 半写损坏**：原子写（临时文件 + rename）保证要么旧版本完整，要么新版本完整，不存在半写状态。
- **pipeline 中途卡死**：提供 `/specpowers.reset` 清理 state + lock，回到 `ready` 重新开始。注意：reset 不清除 `fallback_count`。
- **baseline.json 损坏**：`/specpowers.constitution --force` 强制重建。
- **archive 事务边界**：archive 成功后将 `{stage: "ready", last_archive_ref: <当前HEAD>}` 作为**一次原子写**（临时文件 + rename）更新 state.json。如果 crash 发生在 git commit 之后、state 更新之前，state 仍为 build → 下次启动检测到 feature 已有 archive commit → 提示 `git pull && /specpowers.reset`，不会重复归档。

---

## 7. 分阶段实施路线（构建顺序 + 每阶段验收）

| 阶段 | 范围 | 产物 | 验收标准 |
|---|---|---|---|
| **P0 脚手架** | 目录、`bin/specpowers` 薄壳+`.bat`、`git_util` 安全封装、`lock`（含死锁检测）、`fs_state`、`platform`、`facade` 参数解析 | 可运行 `python bridge/facade.py --help` / 无参冷启建 state | 前置校验（仓库/detached/空仓/git存在/锁）全部生效；subprocess 无 shell 注入；死锁自动恢复；Linux/macOS/Windows 三平台 `bin/specpowers` 可用 |
| **P1 结构门禁主线** | `BaselineScanner` + `StructureGate.extract_signals` + `audit.log` + `ArtifactRegistry` + `Dispatcher.enforce_auto_read` | 基线扫描 + 实时门禁信号提取 + 缺必读拒绝 | 扫出 top_dirs/deps/src_patterns 正确；信号提取准确；缺 constitution 拒绝开工 |
| **P2 constitution+OpenSpec 接线** | `Adapter.speckit` / `Adapter.openspec`（specify/archive delta） | constitution 生成 + 场景 + 常规 archive | 常规流水线可跑通到 archive（delta 合并成功） |
| **P3 优化模式主线** | `ModeController`（判小 prompt/确认/清单/回退）+ 优化 archive stub | 优化模式跑通 + stub 落盘 | bugfix 走优化模式跳过 spec/plan；stub 含 hash+改动范围；回退仅一次且不可 reset 绕过 |
| **P4 能力池+writing-plans 瘦身+五技能** | `Dispatcher` 能力调度 + `plan.md` 归并 + superpowers 五技能接线 | 常规 build 按声明启用 worktree/subagent/TDD | 能力声明驱动行为；优化模式禁 worktree/subagent |
| **P5 基线刷新三路+合体后校验** | refresh 三路（constitution 改动/手动/`git_ref` 相对阈值）+ 合体后校验 + `--force-merge-check` | 刷新与校验生效 | 改 constitution 自动重扫；merge commit/多作者触发校验；force 强制开；基线漂移 warning |
| **P6 抽样审计闭环+判小回归** | `SamplingAuditor` + `judge_tests.json` | 审计闭环 + 测试集 | 不通过率>30% 告警；判小误判<20% 回归门槛 |
| **P7 真实特性验证+多角色验收** | 一个 bugfix（优化）+ 一个小特性（常规）走查 + token 实测 | 验收报告 | 体感顺滑；token 实测；多角色验收清单通过 |

优先级映射（架构 §9）：P0=脚手架/门禁/优化/真实验证；P1=刷新/合体校验/能力池/产物清单；P2=抽样/判小回归/force-merge。

---

## 8. 关键逻辑细节

- **基线扫描**：`ls-tree HEAD` 顶层 → `top_dirs`；依赖清单按已知文件名 glob → `deps`；`src/**/` 取二级目录名聚类 → `src_patterns`。一次性扫死，三路刷新。
- **门禁信号提取**（确定性辅助）：比对 build diff 与 baseline 的 `top_dirs/deps/src_patterns`，输出命中信号列表；**三态判定由 agent 在 build.md 契约内做**（维持零配置，接受不可回归）。
- **判小**：信号集固定 6 个；输出 `[理由：<信号>/...，置信度：<高/中/低>]`；只判"小/非小"二值，其他默认完整流程。
- **验收清单**：Dispatcher 基于需求+对话生成 3-5 条；`<3` 拒绝、`>5` 提示回退；archive 检查全勾选，未全→转人工。
- **合体后校验检测**：`git log <last_archive_ref>..HEAD --merges` 非空 → 多分支；或 author/committer 去重>1 → 多作者；`--force-merge-check` 绕过自动判断强制开。
- **改动范围说明**：`git diff --stat` 作输入 → 由 archive 阶段 agent 生成 3-5 句自然语言（非纯脚本 dump）。
- **锁与原子写**：`.lock` + rename；`state.json` 临时文件 rename 替换。锁含死锁检测（检查持锁进程存活状态）。
- **commit message 格式（优化模式 archive）**：`specpowers(fast): <normalized_feature> — <改动范围摘要（1 句）>`。其中 `normalized_feature` 是入口时经 `normalize_feature()` 处理后的 slug（见 §4.9）。agent 自动生成 message，用户在 archive 交互中可编辑确认。

### 8.1 大仓库性能退化策略

单体大仓（monorepo，文件数 > 10,000 或 `git ls-tree` 输出 > 5,000 行）场景下，基线扫描可能耗时较长。退化策略：

| 阶段 | 正常 | 大仓退化 |
|---|---|---|
| `ls-tree HEAD` | 扫全部顶层目录 | 若输出 > 1000 行，提示用户指定关注子目录（交互式），默认只扫一级目录名（不递归） |
| 依赖清单扫描 | glob 全仓库 | 仅检查仓库根目录下的依赖文件，跳过子项目内部 |
| `git log` 月均估算 | 90 天历史 | 缩短为 30 天（`git log --since=30.days`） |
| 超时兜底 | 无 | 整体扫描超过 60s → 截断，输出已收集的部分结果 + warning |

> 退化策略由 `baseline_scanner.py` 内部自动判断触发。用户也可通过环境变量 `SPECPOWERS_SCAN_DEPTH=shallow` 显式启用。

### 8.2 调试日志

- **默认**（无参数）：仅输出错误和关键状态变更（阶段跃迁、门禁结果）。
- **verbose 模式**（通过环境变量 `SPECPOWERS_VERBOSE=1` 启用）：输出 bridge 内部执行细节（git 命令及参数、文件读写路径、锁获取/释放、信号提取中间结果）。
- **日志目标**：stderr（不污染 stdout 的结构化输出）。
- **audit.log** 不受 verbose 影响，始终按抽样规则写入。

---

## 9. 测试与验证

### 9.1 `judge_tests.json` 格式

```jsonc
{
  "version": 1,
  "description": "判小回归测试集",
  "cases": [
    {
      "id": "J001",
      "requirement": "修复登录页按钮文字拼写错误",
      "expected": "small",
      "signals": ["bugfix", "单文件", "纯文案"],
      "confidence": "高",
      "note": "典型文案修复，应走优化模式"
    },
    {
      "id": "J002",
      "requirement": "新增用户优惠券模块，含数据库表、接口、前端页面",
      "expected": "full",
      "signals": [],
      "confidence": "高",
      "note": "新模块涉及多层，必须走完整流程"
    }
    // ... 共计 10-20 条
  ]
}
```

字段说明：
- `id`：唯一标识，J + 三位数字
- `requirement`：模拟用户需求文本
- `expected`：`"small"` 或 `"full"`
- `signals`：期望命中的判小信号（`expected=full` 时为空数组）
- `confidence`：期望置信度 `"高"/"中"/"低"`
- `note`：补充说明

### 9.2 测试矩阵

| 类型 | 内容 | 达标线 |
|---|---|---|
| 确定性单测 | git_util 安全调用、baseline 扫描、merge-check 检测、checklist 边界、state 原子写、死锁检测 | 全绿 |
| 判小测试集 | `judge_tests.json` 10-20 条 {需求, 期望 small/full, 信号, 备注} | 误判率 < 20% |
| 真实走查 | bugfix→优化模式；小特性→常规模式 | 体感顺滑、产物齐全、可追溯 |
| token 实测 | 常规 vs 优化模式 token 对比 | 优化模式显著省（无 spec/plan） |
| 多角色验收 | 一线/QA/DevEx/EM/可靠性/安全（架构 §8）逐条确认残余风险可接受 | 全部通过 |
| 团队协作测试 | 两人 clone 同仓库，各自走流水线，交叉 archive | baseline 一致，状态互不干扰 |

---

## 10. 实现视角的残余风险（张力）

| 角色 | 顾虑 | 实现怎么接 | 残余 |
|---|---|---|---|
| 一线开发 | 主观门禁跨 agent 不可重复 | 信号提取确定性化 + 抽样审计 | 仍不可回归（设计已接受） |
| QA | 优化模式无 WHEN/THEN 回归锚 | 最小验收清单 + 门禁 + code review | 回归保护弱于常规 |
| DevEx | 三框架升级维护成本 | 判据只依赖 git+通用文件 | CLI 输出语义变更才可能受影响（低概率） |
| EM | `--fast` 滥用（大特性走优化） | >5 清单提示回退 + 判小信号约束 + 测试集回归 | 依赖使用者自律 |
| 可靠性 | 基线一次扫死误杀 | 三路刷新 + git_ref 提示 + 转人工兜底 | 误杀可人工纠正 |
| 安全 | `.specpowers/` 泄露结构 | 仅本地、gitignore、不含密钥 | 需约定不提交敏感基线 |
| **实现新增** | 认知层 prompt 在 host agent 间漂移 | SKILL.md + prompts/ 锁定为 skill，统一加载 | 宿主 agent 不加载则失效 |
| **实现新增** | CLI 输出解析脆弱 | 仅解析 git 稳定输出，不解析框架 stderr | 框架升级改输出语义需跟进 |
| **实现新增** | baseline.json 团队共享冲突 | JSON 格式冲突概率低；冲突时取时间戳更新的版本 | 手动解决仍需 git 基本技能 |
| **实现新增** | 跨平台 agent 行为差异 | 仅使用 Markdown 指令 + Python stdlib，无平台 API | 不同 agent 对同一 prompt 理解可能略有差异 |
| **实现新增** | 大仓扫描性能 | 退化策略自动触发 + 环境变量显式控制 | 退化后扫描精度下降 |

---

## 11. 交付物与确认点

**本方案若确认，落地时我将创建以下文件**（按 P0→P7 顺序）：
- `SKILL.md` + `prompts/` 7 个契约
- `bin/specpowers` + `bridge/`（facade / dispatcher / adapters×3 / modules×6 / core×5）
- `tests/`（单测 + `judge_tests.json`）

---

## 12. 技能调用方式（Skill Invocation）

> SpecPowers 是一个**通用 AI 编码 agent 插件**（非 WorkBuddy 专属）。以 `specpowers.<动作>` 命名空间对外暴露 slash command，WorkBuddy 中用户以 `/specpowers.constitution` 等形式调用；其他平台（Claude Code / Cursor / Copilot）按各自的命令注册机制映射等效调用。
>
> 命名空间 `specpowers.` 对齐 `/speckit.constitution`（框架级技能命名惯例），避免与 superpowers 裸名技能（如 `/brainstorming`）冲突。

| Slash 命令（用户面） | 归类 | 语义 |
|---|---|---|
| `/specpowers.constitution [--force]` | 概念① 项目原则 | 调 `/speckit.constitution` 生成 `constitution.md` + 扫基线；`--force` 强制重建原则并重扫基线（详见 §12.2） |
| `/specpowers.brainstorm "<需求>"` | 概念② 特性流水线 | 启动特性（常规·含探索）：写 feature 名 → `ready`→`brainstorm`，收尾必产 `brief.md` |
| `/specpowers.specify "<需求>"` | 概念② 特性流水线 | 启动特性（跳过探索）或承接 brainstorm：从 `ready` 进入则写 feature 名／从 `brainstorm` 进入则读 `brief.md` → `specify`，产 `spec.md` |
| `/specpowers.fast "<需求>"` | 开关① 优化模式 | 启动特性（优化）：声明 `mode=fast`（stage 留 `ready`），用户写代码后调 `/specpowers.build`；跳判小直进确认 |
| `/specpowers.plan` | 概念② 特性流水线 | 承接 `specify` → `plan`，产 `plan.md`（含能力声明） |
| `/specpowers.build` | 概念② 特性流水线 | 承接 `plan`(full) **或用户已完成编码**（fast 模式，stage=`ready`+`mode=fast`）→ `build` 执行构建+门禁；fast 模式额外要求最小验收清单 3-5 条 |
| `/specpowers.archive [--force-merge-check]` | 概念③ 收尾 | 收尾：常规 delta 合并 / 优化 git commit+stub；`--force-merge-check` 强制开合体后校验（详见 §12.2） |
| `/specpowers.baseline` | 开关② 基线刷新 | 手动重扫结构基线（三路刷新之一），结果写 `baseline.json`，提示用户提交 |
| `/specpowers.reset` | 运维（不计入概念暴露面） | 删 `state.json` + `.specpowers/.lock`，恢复卡死/残留锁；**不重置 fallback_count** |

### 12.1 流水线状态机与阶段命令语义（明确版）

**核心澄清**：每个阶段都是**一条独立显式命令**（`brainstorm`/`specify`/`plan`/`build`），命令名即阶段名，命令本身既标记「上一阶段完成」又触发「本阶段执行」。Dispatcher 按下方「命令→跃迁矩阵」校验 `state.json.stage` 是否处于本命令的合法 `from_stage`；通过后才加载对应 prompt 契约并按**自动读取契约**校验「本阶段必读上游产物是否已产」（缺必读 → 拒绝开工）。每条命令自解释、无「魔法推进」。

**状态机（`state.json.stage` 合法值及跃迁）**：

```
constitution ─▶ ready ─┬─(brainstorm)────────▶ brainstorm ─▶ specify ─▶ plan ─▶ build ─▶ archive ─▶ ready
                       ├─(specify, 跳过探索)─▶ specify ─▶ plan ─▶ build ─▶ archive ─▶ ready
                       └─(fast, 写代码)→(build)▶ build ─▶ archive ──▶ ready
                                （fast 流程：/specpowers.fast → 用户编码 → /specpowers.build
                                  build 中途声明"升级为完整流程" → 回 specify，仅一次）
```

**命令→跃迁矩阵（Dispatcher 校验依据）**：

| 命令 | 参数 | 合法 `from_stage` | 跃迁 `to_stage` | 本阶段必读上游（缺则拒开工） |
|---|---|---|---|---|
| `brainstorm` | "<需求>" | `ready` | `brainstorm` | — |
| `specify` | "<需求>" | `brainstorm` ／ `ready`（跳过探索）／ `build`（仅 fallback 时） | `specify` | `brief.md` **仅当从 `brainstorm` 进入时必读** |
| `plan` | — | `specify` | `plan` | `spec.md` |
| `build` | — | `plan`（full）／ `ready`（fast 模式，**前提：用户已完成编码**） | `build` | `plan.md`（full）／最小验收清单 3-5（fast） |
| `archive` | `[--force-merge-check]` | `build` | `archive` | 代码变更已存在（`git diff --stat HEAD` 有输出或已有 commit） + 实时门禁通过 |
| `constitution` | `[--force]` | `constitution`（首次）/ 任意（需确认，`--force` 跳过） | `ready` + 基线 | — |
| `fast` | "<需求>" | `ready` | `ready`（`mode=fast`） | — |
| `reset` / `baseline` | — | 任意 | 不变 | — |

> 注：
> - `brainstorm`/`specify`/`fast` 为**入口**（写 feature 名 + 落第一站）；`plan`/`build` 为**中段逐段命令**；`archive` 统一收尾。命令名即阶段名，无「无参自动推进」魔法。
> - **`constitution` 中途调用保护**：若当前 `stage` 不是 `constitution`（即已有进行中的 feature 流水线），`/specpowers.constitution` 需**交互确认**"当前 feature `<name>` 的流水线将被放弃，确定重建原则？"——防止误操作丢弃正在进行的工作。`--force` 跳过确认直接执行。
> - **fast 模式关键约束**：`/specpowers.fast "<需求>"` 只声明模式，不执行 build。用户需**先完成编码**，再手动调 `/specpowers.build`。build 命令检测到 `stage=ready, mode=fast` 时进行代码变更检测：`git diff --stat HEAD`（工作区+暂存区 vs HEAD），有输出 → 进入优化模式构建。若无变更 → 提示"未检测到代码变更，请先完成编码后再运行 /specpowers.build"。已 commit 的变更通过 `git diff --stat HEAD~1..HEAD` 兜底检测。
> - 回退为运行时交互（build 中途自然语言"升级为完整流程"），不暴露为技能命令（方案 A）。
> - 可选调试：`/specpowers.status` 打印当前 `stage / mode / fallback_used / fallback_count`（不计入概念暴露面）。

### 12.2 两个 flag 的参数语义（为什么需要）

> 这两个命令是 v1 命令集中**仅有的两个带 flag 的命令**，本质上是「入口护栏（`constitution`）／出口闸门（`archive`）」一对。设计原则一致：**默认省心（零配置），必要时手动 override（人工兜底）**。

#### 12.2.1 `/specpowers.constitution [--force]`

**归属**：概念① 项目原则，项目级**一次性**。
**无参调用做什么**：调 `/speckit.constitution` 生成 `constitution.md`（仅写**质量/测试/UX/性能**四类原则，**不承载结构规则**）；同时 `BaselineScanner` 扫一次结构基线 → `baseline.json`（含 `git_ref` + `constitution_hash`，**一次扫死不增量**，产物提交到 git）。
**前置护栏**：优化模式（`fast`）开工前若 `constitution.md` / 基线缺失，Dispatcher 直接**拒开工**。
**中途调用保护**：若当前有进行中的 feature（`stage != constitution`），调用 `/specpowers.constitution` 需交互确认"当前 feature 将被放弃"；`--force` 跳过确认直接重建。

**`--force` 的含义**：无视「是否已存在产物」，强制重建。

| 维度 | 无 `--force`（默认） | `--force` |
|---|---|---|
| 已存在 `constitution.md` + `baseline.json` | 跳过，不重复生成 | 强制删除重建 + 重扫基线 |
| 典型场景 | 首次冷启 / 正常进入项目 | `constitution.md` 被大改需重置原则，或想强制刷新基线靶子 |

**基线刷新的三路（与 `--force` 的关系）**：① `constitution.md` 改动 lazy 自动重扫；② `/specpowers.baseline` 手动重扫；③ 本命令 `--force` 显式重跑。三条路互不冲突，`--force` 是「我就是要彻底重建护栏」的硬路径。

**产物**：`constitution.md`（仓库根，对外可读）+ `baseline.json`（`.specpowers/` 内部，**提交到 git 供团队共享**）。

#### 12.2.2 `/specpowers.archive [--force-merge-check]`

**归属**：概念③ 收尾，每特性一轮。
**无参调用做什么**：

- **常规模式**：复用 OpenSpec **delta 合并**（只合 diff，不重写整篇），另做桥接四职责：① 清账「build 阶段转人工挂起项」（门禁判定为"转人工"时记录的原因与上下文，存档于 archive 记录中，带未决项不准归档）② `constitution` 原则证据核查（轻量、只查证据是否存在）③ 标记 `plan.md` 完成 + 产 archive 记录 ④ **合体后校验**（自动判断：自上次 archive 以来有 merge commit / 多作者 → 多分支**开**；单分支 → **关**）；archive **只转人工、不打回**。
- **优化模式**（`mode=fast`）：**退化**——`git commit` + OpenSpec `stub`（模式标记 + hash + 改动范围说明）+ 原则核查；跳过 delta 合并 & 合体后校验。
  - commit message 格式：`specpowers(fast): <normalized_feature> — <改动范围摘要 1 句>`（`normalized_feature` 是入口时经 `normalize_feature()` 处理的 slug）
  - agent 自动生成 message，用户在 archive 交互中可编辑确认

**`--force-merge-check` 的含义**：绕过「自动判断」，强制开合体后校验。

| 维度 | 无 `--force-merge-check`（默认·自动判断） | `--force-merge-check` |
|---|---|---|
| 触发逻辑 | 看 git log 自 `last_archive_ref` 以来有无 merge commit / 多作者 | 无视自动判断，直接开 |
| 漏判场景 | rebase / squash / cherry-pick 工作流**没有 merge commit**，会被误判为单分支而关掉校验 | 手动兜底，强制跑合并后结构校验 |
| 用途 | 绝大多数正常多分支合并 | 用 rebase/squash 流的团队、或怀疑有隐性多人改动合并冲突时显式触发 |

**合体后校验的增量性**：以 `state.json.last_archive_ref` 为基线 commit 比对「自上次以来」范围；首轮 `last_archive_ref` 为空则退化为**全量历史对比**；校验完刷新 `last_archive_ref = 当前 HEAD`，保证下一轮范围正确。

#### 12.2.3 一句话对照

| 命令 | flag | flag 本质 | 默认行为 | flag 效果 |
|---|---|---|---|---|
| `constitution` | `--force` | 「重建护栏」 | 已存在就跳过 | 强制重生成原则 + 重扫基线 |
| `archive` | `--force-merge-check` | 「强制出口闸门」 | 自动判断要不要合体后校验 | 无视判断，强制跑合并后结构校验 |

两者正好呼应整体「零配置 + 必要时人工兜底」原则：`--force` 管入口重置，`--force-merge-check` 管出口补漏。

---

## 13. 性能与 SLO

### 13.1 各阶段耗时预估

> 以下为中等规模项目（~500 文件、~200 提交历史）基准值。大仓场景见 §8.1 退化策略。

| 阶段 | 确定性耗时（Python） | Agent 认知耗时 | 合计（用户体感） | 备注 |
|---|---|---|---|---|
| `constitution`（首扫） | 3-15s | 10-30s | **15-45s** | 取决于仓库大小 |
| `constitution`（已存在，跳过） | < 0.5s | — | **< 1s** | 仅校验 hash |
| `brainstorm` | < 1s | 30-120s | **30-120s** | 主要耗时在 agent 探索 |
| `specify` | < 1s | 20-60s | **20-60s** | — |
| `plan` | < 1s | 20-60s | **20-60s** | — |
| `build`（full） | 2-10s | 60-300s | **60-300s** | 含编码+门禁 |
| `build`（fast） | 1-5s | 30-120s | **30-120s** | 跳过 spec/plan |
| `archive` | 1-5s | 10-30s | **10-35s** | — |
| `baseline`（手动刷新） | 3-15s | — | **3-15s** | 纯确定性 |

### 13.2 性能目标

- 任何确定性操作（Python）单次耗时 ≤ 30s（大仓 ≤ 60s）
- baseline.json 文件大小 ≤ 50KB
- audit.log 单条 ≤ 500B，日增量 ≤ 100 条
- `.lock` 死锁检测响应 ≤ 2s

---

## 14. CI/CD 集成

SpecPowers 支持非交互模式，可嵌入 CI 流水线。

### 14.1 非交互模式

通过环境变量启用：

```bash
SPECPOWERS_CI=1                            # 启用 CI 模式（禁止交互式确认，输出 JSON）
SPECPOWERS_VERBOSE=1                       # 可选：输出详细日志到 stderr

# 用法示例：MR 时跑结构门禁
python bridge/facade.py scan --root .       # 基线扫描
python bridge/facade.py gate --root .       # 结构门禁信号提取，输出 JSON 到 stdout
```

CI 模式下：
- 所有交互式确认自动选择默认值（判小→走完整流程；确认→Y）
- 输出格式为 JSON（方便流水线解析）
- 不写 state.json（CI 无状态）
- 错误退出码非零（方便流水线判断失败）

### 14.2 MR 门禁集成示例

```yaml
# .github/workflows/specpowers-gate.yml
specpowers-gate:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - name: SpecPowers 结构门禁
      run: |
        export SPECPOWERS_CI=1
        python specpowers/bridge/facade.py gate --root . --base origin/main
      # 退出码非零 → MR 阻断
```

---

**下一步**：请你确认方案后，我从 **P0 脚手架** 开始落地（先搭可运行的 skill 骨架：SKILL.md + `bridge/` 内部 helper + 安全 git 封装 + 状态机 + 死锁检测），跑通后再逐阶段推进。如需调整模块拆分、接口签名或构建顺序，现在提。
