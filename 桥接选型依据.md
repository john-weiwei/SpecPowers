# SpecPowers 桥接选型依据（澄清"为何 spec-kit 出 constitution/tasks、OpenSpec 出场景/archive"）

> 本文回答一个设计质疑：阶段 2（specify）用的是 OpenSpec 场景格式，而桥接层在 constitution / tasks 上选了 spec-kit，理由是什么，为什么不全用 OpenSpec？
> 结论先行：**桥接层是中立门面，本身不属于任何引擎；选谁做哪段，是"能力匹配 + 省 token"的路由结果，且 constitution 这条是用户显式指令，不是架构偏好。**

---

## 一、先纠正前提：桥接层不"属于"任何一套

桥接层 = Facade（定序）+ Dispatcher（路由）+ 三个 Adapter（翻译）。它把活儿按阶段派给对应引擎，自己不持有逻辑。

实际路由：

| 阶段 | 借用的原生引擎 | 桥接层在此做什么 |
|---|---|---|
| 1. constitution | **spec-kit** `/speckit.constitution` | 触发生成 + 扫描项目结构出"结构基线快照" |
| 2. specify | **OpenSpec** `#### WHEN/THEN` 场景 | 把需求翻译成 OpenSpec 场景格式 |
| 3. plan / tasks | **spec-kit** task 生成（+ superpowers writing-plans 瘦身） | 合并为单一 `plan.md` |
| 4. build | **superpowers** 能力池 | 实时结构门禁 + 按使用者声明路由能力 |
| 5. archive | **OpenSpec** delta 合并 | 复用 OpenSpec 合并 diff + 桥接三职责 |

所以"桥接层选了 speckit"不准确——是 **2 个阶段路由到 spec-kit，2 个路由到 OpenSpec，1 个路由到 superpowers**。

---

## 二、为什么 constitution 用 spec-kit（而非 OpenSpec）

1. **OpenSpec 根本没有 constitution 这个概念 / 命令。** OpenSpec 的 `config.yaml` 只放流程开关（如是否启用 approve 门禁），没有"生成一份以代码质量 / 测试 / UX / 性能为原则的原则文档"的指令。这是**能力缺口，不是偏好**——OpenSpec 原生就做不了这件事。
2. **这是用户 v6 的显式指令**："`/speckit.constitution` 制定以代码质量、测试标准、用户体验一致性和性能要求为重点的原则"。这条是用户拍板的，不是我的架构选择。

→ 结论：constitution 用 spec-kit，**没得选**（能力缺口 + 用户指令双重锁定）。

---

## 三、为什么 tasks 用 spec-kit（而非 OpenSpec）

- spec-kit 的 **task 生成是其明确成熟的职责**（`/speckit.tasks` 或 specify→tasks 流程），产出结构化可执行任务清单。
- OpenSpec 也有 task，但它是 **change 生命周期里的 task**（一个 change 带一组 tasks），格式偏重"变更描述"，且与 change 强耦合，不方便独立抽出来喂给 superpowers 的执行技能。
- 我们要把 superpowers `writing-plans` 瘦身后吸进**同一份** `plan.md`，spec-kit 的任务清单是更干净的底座。

→ 结论：tasks 用 spec-kit 是**合理的能力匹配**，OpenSpec 在此处并非更优解。

---

## 四、为什么场景 + archive 用 OpenSpec（而非 spec-kit）

核心原因只有一个词：**delta（差异）**。

- OpenSpec 是 **change-driven** 模型：基线 spec + 增量 change，archive 时**只合并 diff，不重写整篇 spec**。这是它最值钱的能力，也是本项目省 token 的**最大单一来源**。
- `#### WHEN/THEN` 场景格式**瘦、可机器校验、1:1 映射验收测试**，天然契合"结构门禁 / 实时卡"的需求。
- 若让 spec-kit 跑 specify + archive：spec-kit 的 `spec.md` **不是 delta-native**，每次归档都要**重新整篇输出 spec**，token 直接翻倍，且丢失 change 追溯能力。

→ 结论：场景 + archive 用 OpenSpec，**只为保住 delta 这一个省 token 的杀手锏**。OpenSpec 在此处明显优于 spec-kit。

---

## 五、诚实的张力：这不是能力硬边界

我在 v5 就承认过：这个拆分是**为省 token 做的启发式路由，不是能力硬边界**。完全可行的替代方案有，且各有代价：

| 策略 | 组成 | 优点 | 代价 |
|---|---|---|---|
| **A（当前 v6.2）** | spec-kit 出 constitution+tasks；OpenSpec 出场景+archive | 各取所长；保住 delta 省 token | 两套引擎要学 / 维护；适配器有翻译成本（已由惰性指针消化） |
| **B：spec-kit 跑全程** | constitution→specify→tasks 全用 spec-kit，**仅借 OpenSpec 的 archive** 做 delta | 只学一套引擎，心智负担最低 | 要放弃 OpenSpec 原生场景格式（用 spec-kit 的 spec 模拟）；spec-kit 的 spec 非 delta-native，archive 前需额外转换才能喂给 OpenSpec delta |
| **C：OpenSpec 跑全程** | 场景+archive+change 用 OpenSpec；constitution 手写 / 跳过 | 一套引擎、delta-native | OpenSpec 无 constitution 命令，原则层得**手搓**；task 生成弱于 spec-kit |

---

## 六、结论 & 待你拍板

- **constitution → spec-kit** = 能力缺口 + 你的指令，**不可争议**。
- **tasks → spec-kit** = 成熟职责匹配，**合理**。
- **场景 + archive → OpenSpec** = 只为 **delta 省 token** 这一杀手锏，**合理**。
- 如果你更看重"**只学一套引擎**"，策略 B / C 成立，代价分别是"放弃原生场景格式 + archive 前转换"或"手搓 constitution + 弱 task"。

你倾向保持 A，还是切到 B / C？拍完我把选型依据焊进 `FRAMEWORK.md`，实现阶段照此落地。
