# 多方视角评审 — SpecPowers v2 整合框架

> 目的：不唱赞歌，拉不同岗位的人从自身利益出发表态，暴露真实分歧。
> 评审对象：specpowers/ v2（specify → build → archive + 可选 brainstorm；constitution `<context>` 注入；子代理隔离；2 产物 spec.md + tasks.md）。

## 各角色看法

| 角色 | 核心立场 | 赞成的点 | 担心 / 批评 | 一句原话 |
|---|---|---|---|---|
| 后端开发者（一线） | 支持，但嫌产物太瘦 | 子代理隔离（代码/报错不回灌主控，上下文干净）、按 task 切片、token 真省；WHEN/THEN scenario 写起来清楚 | spec.md + tasks.md 对复杂模块不够——设计决策/接口契约/数据模型没处放；无 design 中间物，跨文件改动易在 task 间失联 | "省 token 我举双手，但一个中等需求就俩文件，真正实现时我得反刍好几遍才摸清边界。" |
| 前端开发者（一线） | 中立偏支持 | Constitution 的 UX 一致性（加载/空/错三态、设计系统）正中痛点；`<context>` 注入比每次手贴规范省事 | 组件库/设计系统是"项目级"，但没说清 web/app/小程序多端怎么分；性能预算把"UI 响应<100ms"和"读 API p95<200ms"混一列，粒度不对 | "UX 那条我喜欢，但把 UI 响应和后端读接口放一个原则里，前端背了后端的锅。" |
| QA 工程师 | 保留（偏担忧） | scenario→自动化测试映射、archive 末尾证据核查（有测试/无 lint/UX 态/性能），是真质量门 | TDD 设成"推荐不强制"是退步——质量底线交给了用户自觉；覆盖率≥80% 是底线但 TDD 可选时怎么保？2 产物里验收标准够 QA 写用例吗 | "原则说每 scenario 都得过测试，结果 TDD 又说可不强制——那'无测试不合并'到底强制还是建议？" |
| 产品经理 PM | 保留（担忧需求侧） | 流程短、双产物轻、迭代快；spec 的 ## Why 能逼着说清动机 | brainstorm 设成"仅模糊才用"——判定权在开发手里，易跳过导致需求假设没被挑战；复杂需求压进 2 文件，PRD 级信息（边界/非功能/竞品）没处放；archive 查的是"原则证据"不是"需求满足度" | "你说模糊才 brainstorm，可开发觉得不模糊我就没得吵了——需求的坑往往就是'以为不模糊'漏掉的。" |
| 技术负责人 Tech Lead | 强支持 | Constitution 强制 + `<context>` 注入，治"规范写了没人看"；dispatcher 兜底防漂移；原则强制流程可选，既稳又不官僚 | `<context>` 注入靠每阶段都记得塞，dispatcher 本身也是 markdown 指令，agent 跑飞谁来兜？archive "注明未达标"算过还是不算过，边界模糊 | "方向百分百认同，就怕 `<context>` 这根绳松一扣就全散——得有机器可查的钩子，不能只靠自觉。" |
| 工程效能 DevEx | 中立偏支持，质疑落地 | 按需加载、子代理隔离、token 预算、ledger 抗 compaction，全是省成本的正确姿势 | dispatcher"按需加载"在真实 agent 上是否真省？多数 agent 一次把相关 skill 全读进来；子代理 diff 总得回主控做 review；token 省 1/3 是估算不是实测，没基线对比 | "架构图漂亮，但我需要数字：同样需求，SpecPowers vs 裸跑 token 差多少？没实测就是信仰。" |
| 工程经理 EM | 中立（看成本与采用） | 原则强制保证产出一致，流程可选降低小需求负担 | 团队要学 dispatcher + 4 skill + constitution 写法，有采用成本；"省 token"省的是 API 账单还是人效没说清；双门禁/archive 核查增加的步骤 ROI 没算 | "别光说省 token，告诉我团队上手要几天、返工率降几个点，我才好拍板推不推。" |

## 团队会吵起来的 5 个点

1. **TDD 强制还是可选** —— QA 要强制，开发要灵活，Tech Lead 默认可选但有底线。质量 vs 速度的老仗。
2. **产物 2 个还是更多** —— 开发/PM/QA 都觉得复杂需求装不下，Tech Lead/EM 满意精简。极简 vs 充分。
3. **brainstorm 默认开还是关** —— PM 怕关了漏需求，开发怕开了拖沓。需求严谨 vs 速度。
4. **`<context>` 注入可靠性** —— Tech Lead 信任，DevEx 质疑无机器钩子。理想 vs 落地。
5. **省 token 是信仰还是实测** —— DevEx/EM 要数字，框架目前只给估算。

## 小结
SpecPowers v2 在"原则强制、流程可选、省 token"的大方向上获得了 Tech Lead / 后端 / DevEx 的买账，但在**质量底线（TDD 可选）、产物充分性（2 文件）、需求严谨（brainstorm 默认关）、落地可靠性（`<context>` 无机器钩子）、成本证据（缺实测）** 五个点上存在跨角色冲突。它不是" unanimously loved"，而是一个需要团队就上述 5 点拍板的、有立场的框架。
