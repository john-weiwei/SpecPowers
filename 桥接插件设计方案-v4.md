# SpecPowers 桥接插件 v4 — 设计方案

> 状态：设计方案待评审
> 定位：桥接插件（非新框架）—— 复用 OpenSpec / spec-kit / superpowers 能力，加一道**严格编码规范门禁**，产物按"价值闸门"瘦身。

---

## 一、需求理解（先对齐，避免跑偏）

把老板的三句话拆成可执行约束：

1. **严格编码规范约束**
   编码标准（命名 / 结构 / 安全 / 错误处理 / 日志）是**唯一强制门禁**，在 build 产出与 archive 处硬校验，违反即打回。
   *注意*：这与之前"TDD / 头脑风暴不强制"不矛盾——强制的是**代码怎么写**，灵活的是**走不走测试流程**。

2. **过程产出物要对项目有价值**
   设一道**价值闸门**：每个产物必须有一个下游消费者（技能或人工节点），驱动不了任何东西的文档一律不产。砍掉 research / data-model / contracts / constitution 长文等仪式性产物。

3. **superpowers 能力全要，但产出要瘦**
   复用 `writing-plans`（瘦身）、`using-git-worktrees`、`subagent-driven-development`、`executing-plans`、`test-driven-development` 五个技能；核心产物只要"够喂饱这些技能"即可，不追求详尽。

---

## 二、架构：阶段流 + 强制门禁 + 技能映射

```
          specify            plan              build                 archive
       ┌──────────┐    ┌──────────┐    ┌──────────────────┐    ┌──────────┐
       │ spec.md  │ →  │ plan.md  │ →  │ 5 superpowers     │ →  │ change   │
       │(OpenSpec │    │(writing- │    │ skills 执行        │    │ diff     │
       │ scenario)│    │ plans瘦) │    │                  │    │(OpenSpec)│
       └──────────┘    └──────────┘    └──────────────────┘    └──────────┘
                                                    ↑                   ↑
                                          ┌─────────────────────────────────┐
                                          │ coding-standards.md（强制门禁） │
                                          │ 子代理产出 + archive 两处硬校验   │
                                          └─────────────────────────────────┘
```

- **Facade + Dispatcher**（延续 v3）：对外只暴露 `specify / plan / build / archive`，内部路由到对应适配器。
- **三框架各司其职**：
  - OpenSpec → spec 场景格式 + change/archive
  - spec-kit → 任务生成逻辑
  - superpowers → 执行纪律五技能

---

## 三、核心产物（只这三个 + archive 记录）

| 产物 | 来源 | 内容（瘦） | 下游消费者 |
|---|---|---|---|
| `spec.md` | OpenSpec | 需求 + `#### WHEN/THEN` 场景 | verify / 验收 / 未来开发 |
| `plan.md` | writing-plans(瘦) + spec-kit | 任务清单：每任务 = 1 子代理单元 + 场景指针 + 验收 + 依赖 + 是否 test-first | subagent / executing / worktree |
| `coding-standards.md` | 桥接层 | 强制编码规范 | 所有产出校验（唯一门禁） |
| archive diff | OpenSpec change | 变更合并记录 | 维护 |

**砍掉的产物**：research / data-model / contracts / constitution 长文 / design 详述 / 多份状态文档。

---

## 四、writing-plans 如何瘦身

superpowers 原版 `writing-plans` 会写：背景长文、方案对比、边界 case 散文、rollback 计划等。瘦身为：

- **目标**（1 行）
- **任务拆解**：每个任务含 `id / 对应 spec 场景指针 / 验收标准 / 依赖 / 是否 test-first`
- **完成定义**（done）

不写长篇 rationale。每个任务必须**自洽到能直接交给 subagent-driven-development 派生子代理**。

---

## 五、五个 superpowers 技能如何被核心产物喂饱

| 技能 | 被什么喂 | 做什么 |
|---|---|---|
| `writing-plans` | —— | 产出 `plan.md`（已瘦身） |
| `using-git-worktrees` | `plan.md` 任务列表 | 每任务一个 worktree 分支 |
| `subagent-driven-development` | `plan.md` 任务 + `spec.md` 场景 + 惰性指针 | 每任务派子代理；产出不回灌主控 |
| `executing-plans` | `plan.md` 依赖序 | 按序编排任务执行 |
| `test-driven-development` | 任务 `test-first` 标记 | 标记的任务先写测试 |

> Context 沿用 v3 的**惰性指针 manifest**：只传文件路径指针，子代理按需拉取，不内联整段 `<context>`。

---

## 六、严格编码规范：强制而非建议

`coding-standards.md` 定义硬约束（示例维度）：

- **结构**：命名 / 目录一致，单一职责
- **错误处理**：禁裸 panic / 未捕获异常；对外接口结构化错误
- **安全基线**：禁硬编码密钥、必须参数校验、SQL 参数化
- **日志**：结构化、禁打敏感字段
- **性能**：禁 N+1、关键路径有超时

**校验点**：子代理产出后 + archive 前。违反 → 打回修复。这是**唯一硬门禁**；TDD / 头脑风暴仍按 v3 可选。

---

## 七、与 v3 的差异

| 项 | v3 | v4 |
|---|---|---|
| 编码规范 | 记录、不强制 | **严格强制（唯一门禁）** |
| superpowers 技能 | 未显式列 | 显式桥接五技能，writing-plans 瘦身 |
| 产物价值 | 未量化 | 价值闸门（每产物必有一消费者） |
| 核心产物 | spec.md + tasks.md | spec.md + plan.md + coding-standards.md |

---

## 八、待拍板

1. **校验时机**：coding-standards 在子代理产出时实时卡，还是只在 archive 卡？（实时卡更省返工 token，但每任务多一次校验调用）
2. **产物合并**：`plan.md` 是否同时吸收 spec-kit 的 task 生成（合并为单一产物），还是保留 `tasks.md` 与 `plan.md` 两份？
3. **TDD 与门禁关系**：标记 `test-first` 的任务，若子代理没先写测试，是否算违反编码规范（硬打回）？
