# SpecPowers 桥接选型总纲（决策：锁定策略 A）

> 状态：**决策锁定**。基于 `桥接插件设计方案-v6.3` 给出整体选型依据与强说服力论证。
> 本文目的：回答"为什么是这样做的"，并正面回应所有被否决的替代方案。非技术实现文档。
> 关联：`桥接插件设计方案-v6.3（锁定版）.md`、`桥接选型依据.md`（单点澄清）、`角色视角评审.md`（对抗性校验）。

---

## 0. 一句话决策

**SpecPowers 是一条薄桥接层（Facade + Adapter + Dispatcher），不做第四套框架；把三套已成熟的原生引擎按"能力匹配 + 省 token"路由到 6 段（项目级 constitution 一次 + 特性级 5 阶段，含编号可选的 brainstorm），并补齐它们都没有的两条硬约束（结构一致性门禁、实时卡转人工）。策略 A 锁定：spec-kit 出 constitution+tasks，OpenSpec 出场景+archive，superpowers 出执行纪律（含高权重的头脑风暴）。**

---

## 1. 根原则：适配优于重写（为什么是桥接，不是新框架）

这是整个设计的地基，也是后端架构的基本纪律：**当已有健壮引擎可适配时，重写是技术负债，不是创新。**

- OpenSpec / spec-kit / superpowers 三套都是开源、经过真实项目检验的规格驱动方案，各自有成熟的 spec 解析、任务生成、执行技能。
- 若另造"SpecPowers 框架"（即早期 v2 思路），等于用一份未经验证的 markdown 指令集，重新实现它们已做对的事——**丢失原生成熟度 + 新增维护成本 + 与上游脱节**。
- 桥接层只做三件它们做不好/做不了的事：**共享契约定义、适配器翻译、按需路由 + 两条独有门禁**。逻辑零重写，上游升级即白嫖。

→ 这是被否决"v2 新框架"方案的根本理由，也是策略 A/B/C 共同的前提。

---

## 2. 整体拓扑与阶段路由

```
        ┌─────────────────────────────────────────┐
        │            SpecPowers Facade            │  门面：定序 + 触发
        ├─────────────────────────────────────────┤
        │  Dispatcher（按阶段路由 + 惰性指针注入） │
        ├──────────┬──────────┬───────────────────┤
        │ Adapter  │ Adapter  │ Adapter           │  适配器：格式翻译
        │ OpenSpec │ spec-kit │ superpowers       │
        └──────────┴──────────┴───────────────────┘
              │           │            │
        ┌─────┴────┐ ┌────┴─────┐ ┌───┴──────────┐
        │ OpenSpec │ │ spec-kit │ │ superpowers  │  原生引擎（不改动）
        │ 引擎     │ │ 引擎     │ │ 技能集       │
        └──────────┘ └──────────┘ └──────────────┘

   桥接层独有：结构基线扫描 + 实时门禁(三态) + 转人工
```

| 层级 | 阶段 | 路由引擎 | 该引擎在此的不可替代性 |
|---|---|---|---|
| 项目级（一次性） | constitution | **spec-kit** | OpenSpec 无 constitution 命令；spec-kit `/speckit.constitution` 原生生成原则文档 |
| 特性级 | 1. brainstorm（可选，高权重） | **superpowers** | 需求探索是 superpowers 核心环节；编号在册但跑不跑由使用者决定 |
| 特性级 | 2. specify | **OpenSpec** | `#### WHEN/THEN` 场景瘦、可机器校验、1:1 映射验收 |
| 特性级 | 3. plan / tasks | **spec-kit** | task 生成是其成熟职责；OpenSpec task 耦合 change 生命周期，不便独立抽取 |
| 特性级 | 4. build | **superpowers** | 执行纪律（worktree/subagent/executing/TDD）是 superpowers 的立身之本 |
| 特性级 | 5. archive | **OpenSpec** | change-driven **delta 合并**——整个设计省 token 的最大单一来源 |

---

## 3. 逐引擎选型理由（及"为什么不是别的"）

### 3.1 constitution → spec-kit（不可争议）
- **能力缺口**：OpenSpec 根本没有 constitution 概念；superpowers 有质量纪律但无"生成原则文档"的指令。
- **用户显式指令**：v6 要求"`/speckit.constitution` 聚焦质量/测试/UX/性能"。
- 双重锁定，无替代。

### 3.2 场景 + archive → OpenSpec（唯一最优）
- **delta 是杀手锏**：OpenSpec 的 change-driven 模型，archive 只合并 diff，不重写整篇 spec。spec-kit 的 `spec.md` 非 delta-native，每次归档需重输整篇 → **token 直接翻倍 + 丢变更追溯**。
- **场景格式可测**：`#### WHEN/THEN` 天然喂结构门禁与验收，比对 prose 式 spec 省解析成本。
- 若改投 spec-kit（策略 B）：需额外把 spec-kit spec 转成 OpenSpec delta 格式才能借 archive，转换本身吃 token 且易错。

### 3.3 tasks → spec-kit（合理匹配）
- spec-kit task 生成成熟、结构化；与 superpowers `writing-plans` 瘦身后合并为单一 `plan.md`，底座干净。
- OpenSpec 的 task 附属在 change 内，格式偏"变更描述"，抽出来喂执行技能别扭。

### 3.4 build → superpowers（立身之本）
- worktree 隔离、subagent 上下文隔离、executing 编排、TDD 纪律——这四个是 superpowers 独有且成熟的执行能力，三套里只有它齐活。

---

## 4. 桥接层独有能力的正当性（这才是桥接的价值，不是重复造轮子）

**关键论证**：两条硬约束（结构一致性门禁、实时卡转人工）是 **OpenSpec / spec-kit / superpowers 三套原生都没有的能力**。桥接层不是在复制它们的逻辑，而是在**填补一个它们共同的盲区**——

> AI 生成代码"无中生有"地新建目录、引入项目没有的架构/依赖，是三套框架的通病，没有任何一套内置约束。

- 结构一致性门禁：基线由插件扫描**真实项目结构**生成快照，子代理每次产出实时比对。这是把"AI 不乱建目录"从口号变成机器可查的硬规则。
- 三态结果（通过 / 转人工 / 打回）：拿不准不自动决策，挂起交人——同时治了"误报打回拖节奏"和"误放导致乱建目录"两个病。
- 这两条约束是 SpecPowers 区别于"裸用三套框架"的**唯一不可替代价值**，也正是用户 v5-⑥ 澄清的"代码必须契合现有结构"诉求的落地。

---

## 5. 省 token 的叠加机制（结构性，非玄学）

设计层面的 token 节省来自 **4 层结构性机制叠加**，而非单点优化：

| 层 | 机制 | 省在哪 |
|---|---|---|
| L1 | 不重写逻辑，复用原生引擎 | 不为第四套框架写解析/生成代码 |
| L2 | 惰性指针（传路径不内联 constitution） | 不每阶段重复塞整段原则 |
| L3 | 价值闸门：只留 4 产物 | 砍掉 research/data-model/contracts/quickstart 等黑洞文档 |
| L4 | OpenSpec delta 合并 | archive 不重写整篇 spec（单点最大节省） |

**诚实声明（回应 DevEx 的质疑）**：上述 L1–L4 是**架构层面的结构性节省**，逻辑上成立；但具体节省比例（如"省 1/3"）目前是**估算，非实测**。闭环动作已在 v6.2 第 9 节：最小骨架跑真实特性后回填实测数字。不把估算当结论，是可信度的底线。

---

## 6. 被否决的替代方案（各自的死穴）

| 方案 | 做法 | 死穴 |
|---|---|---|
| **v2 新框架** | 另写一套 SpecPowers 框架 | 重写三套已成熟的引擎，丢上游成熟度 + 维护成本 + 与社区脱节 |
| **纯 superpowers** | 只用 superpowers 全流程 | 产出过多（你最初吐槽点）、无 delta、无结构门禁、架子重 |
| **策略 B（spec-kit 全程）** | 只借 OpenSpec archive | 放弃原生场景格式；archive 前需把 spec-kit spec 转 OpenSpec delta，转换吃 token 且易错 |
| **策略 C（OpenSpec 全程）** | constitution 手写/跳过 | OpenSpec 无 constitution 命令，原则层得手搓；task 生成弱于 spec-kit |
| **build 固定四连** | 硬编码 worktree+subagent+executing+TDD | 与"流程可选、使用者作主"自相矛盾；小改动也硬开重架子，纯浪费 |

→ 策略 A 是在"省 token / 能力匹配 / 使用者自治 / 不重写"四个约束下的**帕累托最优**：每让一步都会撞上上述某个死穴。

---

## 7. 对抗性校验回顾（设计已 survive 多视角炮火）

引用 `角色视角评审.md` 中不肯附和的质疑，及本设计如何化解：

- **QA："TDD 说推荐不强制，但原则写无测试不合并——矛盾"** → v6 已厘清：TDD 可选，但"代码结构怎么写"强制；archive 做原则**证据核查**（查测试是否执行），不卡流程。矛盾解除。
- **PM："brainstorm 默认关，判定模糊的权在开发手里"** → v6.2 把头脑风暴定为**使用者显式触发**，且位置在 specify 前；复杂需求由使用者决定是否榨清。权交回使用者，符合"流程可选"。
- **DevEx："省 token 是估算不是实测"** → 见 §5 诚实声明 + 实测闭环计划。
- **Tech Lead："`<context>` 注入靠自觉易漂移"** → v3 已改为**惰性指针**（路径非内容），且 build 实时门禁是机器可查的硬钩子，不靠自觉。
- **EM："ROI 看省 API 费还是人效"** → 桥接层降低的是"学一套新 dispatcher"的接入成本（复用三套原生），且产物收敛直接砍掉无消费者文档的工时浪费。

→ 设计经多方视角挑战后，核心未被推翻，仅细化（v6.1/v6.2 两次修正），说明结构稳健。

---

## 8. 风险与未决项（不粉饰）

1. **双引擎维护成本**：策略 A 需维护 OpenSpec + spec-kit 两套适配器。缓解：适配器只做格式翻译，逻辑零重写。
2. **token 节省未实测**：§5 已声明，待最小骨架实测回填。
3. **跨框架 diff 合并**：目前 archive 用 OpenSpec 原生 delta；若未来 spec-kit 侧变更需并入同一条 spec 线，翻译边界待定义（v6.2 第 9 节待办）。
4. **结构基线快照保鲜**：项目结构演进后快照需重扫，否则门禁比对失真。缓解：constitution 阶段每次重扫。

---

## 9. 结论

策略 A 不是"我觉得好看"，而是四个硬约束（**不重写 / 能力匹配 / 省 token / 使用者自治**）共同挤压出的唯一不撞死穴的解。桥接层的价值不在复制三套框架，而在补齐它们都没有的**结构一致性门禁**——这正是你"生成代码必须契合现有项目结构、禁无中生有"诉求的唯一可机器化落地方式。

落地衔接：详见 `桥接插件设计方案-v6.2` 第 9 节实现待办；选型锁定后，实现阶段不再 reopen 路由决策。
